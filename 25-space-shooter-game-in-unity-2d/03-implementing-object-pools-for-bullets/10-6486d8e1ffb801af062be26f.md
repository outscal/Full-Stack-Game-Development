# [Quiz] GetBullet method in bullet pool

GetBullet method in bullet pool


## Questions


### Q1. What does the GetBullet() method in the BulletPool class do?
- Retrieves an unused bullet object from the pool, marking it as used.
- Creates a new bullet object and adds it to the pool for later use.
- Deactivates a used bullet object, returning it to the pool for reuse.
- Checks if a bullet object is available and returns it if unused; otherwise, creates a new bullet object. **(correct)**

### Q2. What does the method GetBullet() return?
- A PooledBullet
- A Generic Type
- A Bullet Prefab
- A BulletController **(correct)**
