# [Quiz] Return Bullet to object pool

Return Bullet to object pool


## Questions


### Q1. What is the purpose of the ReturnBullet() method in the BulletPool class?
- To remove a bullet object from the pool permanently.
- To mark a used bullet object as unused and available for reuse. **(correct)**
- To check if a bullet object is in use and return it if not.
- To check if a bullet object is in use and return it if not.

### Q2. How does the ReturnBullet(BulletController bullet) method mark a bullet as not being used?
- By setting the isUsed property of the PooledBullet to true.
- By setting the isUsed property of the PooledBullet to false. **(correct)**
- By removing the bullet from the pooledBullets collection.
- By creating a new PooledBullet and replacing the existing one.
