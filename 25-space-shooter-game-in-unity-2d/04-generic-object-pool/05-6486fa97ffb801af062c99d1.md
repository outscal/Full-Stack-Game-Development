# [Quiz] Why do we need a Generic Object Pool

Why do we need a Generic Object Pool


## Questions


### Q1. True or False: A generic object pool can be used to manage objects of any type.
- True **(correct)**
- False

### Q2. Which of the following scenarios would be more suitable for using a generic object pool rather than a normal object pool?
- When there is a need to pool objects of multiple types or classes. **(correct)**
- When the objects have complex initialization requirements.
- When the objects have a predictable and fixed lifetime.
- When there is a need for strict compile-time checking of object types.
