# [Quiz] ReturnItem in Generic Object Pool

ReturnItem in Generic Object Pool


## Questions


### Q1. What is the purpose of the ReturnItem(T item) method in the GenericObjectPool<T> class?
- It returns an item to the object pool after it has been used. **(correct)**
- It creates a new item and adds it to the object pool.
- It removes an item from the object pool.
- It marks an item as used in the object pool.

### Q2. How does the ReturnItem(T item) method mark an item as not being used?
- By setting the isUsed property of the PooledItem<T> to true.
- By removing the item from the pooledItems collection.
- By setting the isUsed property of the PooledItem<T> to false. **(correct)**
- By creating a new PooledItem<T> and replacing the existing one.
