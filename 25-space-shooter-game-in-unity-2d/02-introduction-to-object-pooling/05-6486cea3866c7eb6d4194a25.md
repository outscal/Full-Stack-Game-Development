# [Quiz] Types of Object Pools

Types of Object Pools


## Questions


### Q1. What is the main difference between dynamic object pooling and normal object pooling?
- Dynamic object pooling supports a variable number of objects, while normal object pooling has a fixed number of objects. **(correct)**
- Dynamic object pooling is faster than normal object pooling.
- Dynamic object pooling only works for specific types of objects, while normal object pooling works for any object type.
- Dynamic object pooling is only used in single-player games, while normal object pooling is used in multiplayer games.

### Q2. In dynamic object pooling, what happens when the pool runs out of available objects?
- The game waits until an object becomes available.
- The pool dynamically creates and adds more objects to the pool **(correct)**
- The game crashes due to an out-of-memory error.
- The oldest object in the pool is removed to make space for a new object.
