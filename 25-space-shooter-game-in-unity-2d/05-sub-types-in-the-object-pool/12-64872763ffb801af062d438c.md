# [Quiz] Creating PowerUp Pool

Creating PowerUp Pool


## Questions


### Q1. What does the CreateItem<T>() method in the PowerUpPool class do?
- It creates a new instance of a power-up of the specified type T. **(correct)**
- It retrieves an available power-up of the specified type T from the object pool.
- It returns a new power-up of the specified type T if no available power-ups are found in the pool.
- It removes the oldest power-up from the pool to make room for a new power-up of the specified type T.

### Q2. In the GetPowerUp<T>(PowerUpData powerUpData) method, what is the purpose of the where T : IPowerUp constraint?
- It ensures that the T type parameter is a reference type.
- It makes sure that the T type is of PowerUpController.
- It allows the use of any type for the T type parameter.
- It enforces the inheritance of the T type parameter from the IPowerUp interface. **(correct)**
