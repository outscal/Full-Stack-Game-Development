# [Quiz] Understanding Sub-Types in Generic Object Pool

Understanding Sub-Types in Generic Object Pool


## Questions


### Q1. In the context of creating a power-up pool with different kinds of power-ups, what limitation does the original generic object pool script have?
- It only allows storing and retrieving a single type of power-up. **(correct)**
- It does not support object pooling for power-ups.
- It does not provide a mechanism for reusing power-up instances.
- It lacks the necessary properties and methods for managing power-up pooling.

### Q2. Which modification is needed in the original generic object pool script to support storing different kinds of power-ups?
- Modify the generic type constraint to allow multiple types. **(correct)**
- Introduce a separate pool for each power-up type.
- Implement an interface for each power-up type to enable pooling.
- Create a derived class for each power-up type to extend the object pool.
