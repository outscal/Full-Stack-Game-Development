# [Quiz] Adding References to PowerUpPool methods

Adding References to PowerUpPool methods


## Questions


### Q1. What is the role of the dataToUse field in the PowerUpPool class?
- It stores the data associated with the power-up being retrieved. **(correct)**
- It keeps track of the number of power-ups in the pool.
- It manages the synchronization of power-up retrieval in a multi-threaded environment.
- It determines the maximum limit of power-ups that can be stored in the pool.

### Q2. What happens if the GetPowerUp<T>(PowerUpData powerUpData) method is called with a power-up type that is not supported in the CreateItem<T>() method?
- The method throws a NotSupportedException. **(correct)**
- The method returns null.
- The method returns a default instance of the power-up type.
- The method creates a new instance of the power-up type.
