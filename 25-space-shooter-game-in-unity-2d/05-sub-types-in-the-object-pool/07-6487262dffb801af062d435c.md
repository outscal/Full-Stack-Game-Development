# [Quiz] Refactoring Generic Object Pool

Refactoring Generic Object Pool


## Questions


### Q1. What modification has been made in the GenericObjectPool script to enable storing different kinds of power-ups?
- Added a new method called GetItem<U>() with a type parameter U **(correct)**
- Added a new class called PooledItem<T> to handle different power-up types
- Changed the generic type constraint from where T : class to where T : IPowerUp
- Modified the CreateItem() method to support multiple power-up types

### Q2. How does the GetItem<U>() method in the modified script differ from the original GetItem() method?
- It allows retrieving any type of object, not just power-ups
- It checks if an item is available and matches a specific type U before returning it **(correct)**
- It creates a new instance of the specified power-up type U if no item is available
- It throws a NotSupportedException if the specified type U is not implemented in the derived class
