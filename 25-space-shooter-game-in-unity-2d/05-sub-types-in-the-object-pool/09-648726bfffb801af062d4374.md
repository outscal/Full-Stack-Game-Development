# [Quiz] Error Fixing after adding new Generic Type

Error Fixing after adding new Generic Type


## Questions


### Q1. What is the behavior of the CreateNewPooledItem<U>() method when creating a new pooled item for a sub-type?
- It creates a new instance of the base type instead of the sub-type.
- It creates a new instance of the sub-type and adds it to the pool. **(correct)**
- It throws an exception since sub-types cannot be created within the generic object pool.
- It retrieves an existing pooled item of the sub-type if available; otherwise, it creates a new instance of the sub-type.

### Q2. When using the generic object pool with sub-types, what is the benefit of using the GetItem<U>() method instead of the base GetItem() method?
- The GetItem<U>() method allows for dynamic selection of sub-types during runtime. **(correct)**
- The GetItem<U>() method is not applicable when dealing with sub-types.
- There is no additional benefit.
- The GetItem<U>() method can only retrieve items of the specified sub-type.
