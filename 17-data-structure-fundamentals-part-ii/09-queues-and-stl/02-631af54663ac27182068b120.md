# [Quiz] STL Queue

STL Queue


<div class="outscal-quiz" data-total="8">

<div class="oq-q" data-idx="0" data-answers="3">
<h3>Q1. Which of the following data structure is used in BFS(Breadth First Search)?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Array </button>
  <button type="button" class="oq-opt" data-i="1">Linked List </button>
  <button type="button" class="oq-opt" data-i="2">Stack </button>
  <button type="button" class="oq-opt" data-i="3">Queue</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="2" hidden>
<h3>Q2. Which of the following data structure is used in DFS(Depth First Search)?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Array</button>
  <button type="button" class="oq-opt" data-i="1">Linked List </button>
  <button type="button" class="oq-opt" data-i="2">Stack </button>
  <button type="button" class="oq-opt" data-i="3">Queue</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="2" data-answers="0" hidden>
<h3>Q3. Which among the following data structure may give overflow error, even though the current number of elements in it, is less than its size: </h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Simple Queue </button>
  <button type="button" class="oq-opt" data-i="1">Circular Queue </button>
  <button type="button" class="oq-opt" data-i="2">Stack </button>
  <button type="button" class="oq-opt" data-i="3">None</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="3" data-answers="0" hidden>
<h3>Q4. An implementation of a queue Q, using two stacks S1 and S2, is given below: 
void insert(Q, x) {
   push (S1, x);
}
  
void delete(Q){
   if(stack-empty(S2)) then 
      if(stack-empty(S1)) then {
          print(“Q is empty”);
          return;
      }
      else while (!(stack-empty(S1))){
          x=pop(S1);
          push(S2,x);
      }
   x=pop(S2);
}

Let n insert and m (&lt;=n) delete operations be performed in an arbitrary order on an empty queue Q. Let x and y be the number of push and pop operations performed respectively in the process. Which one of the following is true for all m and n?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">n+m &lt;= x &lt; 2n and 2m &lt;= y &lt;= n+m</button>
  <button type="button" class="oq-opt" data-i="1"> n+m &lt;= x &lt; 2n and 2m&lt;= y &lt;= 2n </button>
  <button type="button" class="oq-opt" data-i="2">2m &lt;= x &lt; 2n and 2m &lt;= y &lt;= n+m </button>
  <button type="button" class="oq-opt" data-i="3">2m &lt;= x &lt;2n and 2m &lt;= y &lt;= 2n</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="4" data-answers="2" hidden>
<h3>Q5. What does the below function do? (Assume that IntQueue is an integer queue.) 
void fun(int n)
{
    IntQueue q = new IntQueue();
    q.enqueue(0);
    q.enqueue(1);
    for (int i = 0; i &lt; n; i++)
    {
        int a = q.dequeue();
        int b = q.dequeue();
        q.enqueue(b);
        q.enqueue(a + b);
        print(a);
    }
}</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Prints numbers from 0 to n-1 </button>
  <button type="button" class="oq-opt" data-i="1">Prints numbers from n-1 to 0 </button>
  <button type="button" class="oq-opt" data-i="2">Prints first n Fibonacci numbers </button>
  <button type="button" class="oq-opt" data-i="3">Prints first n Fibonacci numbers in reverse order.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="5" data-answers="0" hidden>
<h3>Q6. Consider the following statements: 
i.   First-in-first out types of computations are efficiently supported by STACKS.
ii.  Implementing LISTS on linked lists is more efficient than implementing LISTS on
     an array for almost all the basic LIST operations.
iii. Implementing QUEUES on a circular array is more efficient than implementing QUEUES
     on a linear array with two indices.
iv.  Last-in-first-out type of computations are efficiently supported by QUEUES.

Which of the following is correct?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">(iii) is true</button>
  <button type="button" class="oq-opt" data-i="1">(i) and (ii) are true </button>
  <button type="button" class="oq-opt" data-i="2">(iii) and (iv) are true </button>
  <button type="button" class="oq-opt" data-i="3">(ii) and (iv) are true</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="6" data-answers="0" hidden>
<h3>Q7. What does the function queue::back() do?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Returns a reference to the last element of the queue. </button>
  <button type="button" class="oq-opt" data-i="1">Returns a reference to the first element of the queue. </button>
  <button type="button" class="oq-opt" data-i="2">Returns the size of the queue. </button>
  <button type="button" class="oq-opt" data-i="3">Returns whether the queue is empty.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="7" data-answers="2" hidden>
<h3>Q8. What does the function queue::swap() do?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Insert a new element into the queue container, the new element is added to the end of the queue. </button>
  <button type="button" class="oq-opt" data-i="1">Deletes the first element of the queue. </button>
  <button type="button" class="oq-opt" data-i="2">Exchange the contents of two queues but the queues must be of the same type, although sizes may differ. </button>
  <button type="button" class="oq-opt" data-i="3">None</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">8</span>.</p>
</div>
</div>
