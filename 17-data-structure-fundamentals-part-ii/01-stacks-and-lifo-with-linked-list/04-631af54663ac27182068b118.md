# [Quiz] Stack using Linked List

Stack using Linked List


## Questions


### Q1. Process of inserting an element in stack is called ____________ .
- Create 
- Push  **(correct)**
- Evaluation 
- Pop

### Q2. Which of the following is not an application of stack?
- Evaluation of postfix expression  
- Reverse a string
- Implementation of Recursion 
- Job scheduling **(correct)**

### Q3. Pushing an element into stack already having five elements and stack size of 5, then stack becomes ___________.
- Overflow  **(correct)**
- Crash 
- Underflow 
- User flow

### Q4. The operations of the stack performed in which order?
- FIFO 
- LIFO  **(correct)**
- FILO 
- LILO

### Q5. Consider the usual algorithm for determining whether a sequence of parentheses is balanced. The maximum number of parentheses that appear on the stack AT ANY ONE TIME when the algorithm analyzes: (()(())(()))?
- 1   
- 2
- 3 **(correct)**
- 4 or more

### Q6. What is the value of the postfix expression 6 3 2 4 + – *? 
- 1 
- 40
- 73
- -18 **(correct)**

### Q7. Convert the following infix expression to its equivalent postfix expression -           (A + B^ D) / (E – F) + G 
- ABD+^EF/-G+ 
- ABD^+EF/-G+ 
- ABD^+EF-/G+  **(correct)**
- ABD+^EF-/G+

### Q8. What is the time complexity of push() and pop() if the stack is implemented through linked list?
- O(1) for insertion and O(n) for deletion 
- O(n) for insertion and O(1) for deletion 
- O(n) for insertion and O(n) for deletion 
- O(1) for insertion and O(1) for deletion **(correct)**
