# [Quiz] Stack using Array

Stack using Array


## Questions


### Q1. Process of removing an element from stack is called __________.
- Create 
- Push 
- Evaluation 
- Pop **(correct)**

### Q2. Consider the following operations on a stack of size 3 :- push(1), pop(), push(2), push(3), pop(), push(4), pop(), pop(), pop(), push(5). Which of the following statements is correct?
- Overflow occurs 
- Underflow occurs  **(correct)**
- No overflow occurs and all operations are performed smoothly 
- None of the above

### Q3. Which of the following is an infix expression?
- A+B*C  **(correct)**
- +A*BC 
- ABC+* 
- None of the above

### Q4. When the user tries to delete the element from the empty stack then the condition is said to be a ____.
- Overflow 
- Underflow  **(correct)**
- Garbage collection 
- None of the above

### Q5. What is the minimum number of stacks required to implement the queue?
- 3
- 4
- 2 **(correct)**
- 1

### Q6. The time complexity for all push(), pop(), and peek() operations of stack implemented using array is
- O(n) 
- O(n^2) 
- O(logn) 
- O(1) **(correct)**

### Q7. Convert the following prefix expression to its equivalent infix expression:    *- A / B C - / A K L
- (A/(B-C))*((A/K)-L) 
- ((A-(B/C))*((A/K)-L))  **(correct)**
- ((A-(B/C))/((A-K)*L)) 
- ((A/(B-C))*((A-K)/L))

### Q8. What is the time complexity of findMiddle() if the stack is implemented through array?
- O(nlogn)  
- O(n) 
- O(n^2)  
- O(1) **(correct)**
