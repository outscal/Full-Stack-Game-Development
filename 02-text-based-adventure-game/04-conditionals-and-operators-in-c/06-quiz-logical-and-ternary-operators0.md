# Quiz - Logical and ternary operators

Quiz - Logical and ternary operators


## Questions


### Q1. int a = 5, b = 10;
bool result = (a > 3) && (b < 5);


What will be the value of 'result'?
- true
- false
- The code won't compile
- 0 **(correct)**

### Q2. int x = 10, y = 20;
int z = (x > y) ? x : y;


What value will be assigned to z?
- 10
- 20 **(correct)**
- The code won't compile
- 0

### Q3. int x = 5, y = 10;
int z = (x > 3) && (y < 15) ? 100 : 200;


What value will be assigned to z?
- 100 **(correct)**
- 200
- The code won't compile
- 0

### Q4. int p = 15, q = 25, r = 30;
bool result = (p > q) || (r < q) && (r > p);


What will be the value of result?
- true
- false
- The code won't compile
- 0 **(correct)**

### Q5. int x = 10, y = 20, z = 30;
int result = (x > y) ? (y > z ? y : z) : (x > z ? x : z);


What value will be assigned to result?
- 10
- 20
- 30 **(correct)**
- The code won't compile
