**ASSIGNMENT LINK**: [https://outscal.com/compiler/text-based-adventure-game](https://outscal.com/compiler/text-based-adventure-game)



## TASKS:

- These are some very simple tasks just to get you accustomed to using loops:

1. 1. Write a **for loop** which will run **10** times and call the `shooting()` function inside it.
  2. Write a **while loop** that will run **5** times and call the `healing()` function inside it.
  3. Write a **do-while loop** that will run **7** times and call the `attacking()` function inside it.

- The `shooting()`, `healing()`, `attacking()` functions should only have a debug statement inside them which should print:
- - "player is shooting"
  - "player is healing"
  - "player is attacking"



## Submission

- Save the code in the above compiler link.
- Submit the Submission Link from the compiler.
