# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. How does a doubly linked list facilitate navigation in a playlist feature for a video player application?
- It allows linear access to the playlist in both directions. **(correct)**
- It provides a random access model similar to arrays.
- It restricts navigation to only the next item.
- It allows navigation but only in a forward direction.

### Q2. In terms of memory consumption, how does a doubly linked list compare to a singly linked list when storing the same number of nodes?
- Doubly linked lists use less memory.
- Doubly linked lists use the same amount of memory.
- Doubly linked lists use more memory due to additional pointers. **(correct)**
- The memory usage is unpredictable and depends on the node data size.

### Q3. When would a singly linked list be preferred over a doubly linked list in game development?
- When memory usage is a critical concern.
- When only forward traversal is needed.
- When the complexity of node manipulation needs to be minimized.
- All of the above. **(correct)**

### Q4. What is the time complexity of deleting a node from the middle of a doubly linked list if the node to be deleted is known?
- O(1), as direct links to previous and next nodes allow immediate removal. **(correct)**
- O(log n), as a binary search is required to locate the node.
- O(n), as it requires traversal from the head to the node.
- O(n^2), due to repeated operations for each node deletion.

### Q5. Which of the following is a disadvantage of singly linked lists compared to doubly linked lists?
- Singly linked lists require more memory
- Singly linked lists cannot be traversed in both directions **(correct)**
- Singly linked lists are slower in insertion and deletion operations
- Singly linked lists have higher complexity in searching for elements
