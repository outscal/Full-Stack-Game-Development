# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. How does a doubly linked list facilitate navigation in a playlist feature for a video player application?
- It allows linear access to the playlist in both directions. **(correct)**
- It provides a random access model similar to arrays.
- It restricts navigation to only the next item.
- It allows navigation but only in a forward direction.

### Q2. In game development, how might a doubly linked list be utilized for a character’s inventory system?
- To allow items to be used only once.
- To ensure items are stored based on their rarity.
- To navigate through the inventory list forwards and backwards, facilitating quick access to next and previous items. **(correct)**
- To automatically sort inventory items as they are added.

### Q3. A flight booking system requires a data structure to store ticket bookings, which allows for fast random access and frequent updates as flights are booked or canceled. What is the best choice?
- Arrays
- Vectors **(correct)**
- Single Linked Lists
- Doubly Linked Lists

### Q4. A real-time stock trading application needs to handle a high volume of trades where the main requirement is to append new trades and occasionally process or remove trades from the beginning of the list. What data structure would you use?
- Arrays
- Vectors
- Single Linked Lists **(correct)**
- Doubly Linked Lists

### Q5. In a real-time strategy game, you need to manage a fixed set of resources that players can collect. Each resource's count has to be accessed and updated rapidly as players gather or spend them during gameplay. The counts are displayed on the game UI and must be updated instantly to reflect changes caused by player actions.

Which data structure is most efficient for managing the counts of fixed resources in a real-time strategy game, ensuring quick access and update?
- Arrays **(correct)**
- Vectors
- Single Linked Lists
- Doubly Linked Lists
