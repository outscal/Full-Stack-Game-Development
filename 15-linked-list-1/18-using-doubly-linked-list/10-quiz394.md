# [⏳Quiz]

[⏳Quiz]


<div class="outscal-quiz" data-total="9">

<div class="oq-q" data-idx="0" data-answers="3">
<h3>Q1. What are the ways of implementing linked list?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Array implementation</button>
  <button type="button" class="oq-opt" data-i="1">Linked-list implementation</button>
  <button type="button" class="oq-opt" data-i="2">Cursor implementation</button>
  <button type="button" class="oq-opt" data-i="3">All of the above</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="1" hidden>
<h3>Q2. The function of the given Linked List with the first node as the head is:-

void funx1(struct node* head)
{
  if (head == NULL)
    return;
  funx1 (head -&gt; next) ;
  printf (&quot; %d  &quot;, head -&gt; data ) ;
}</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">It will print all the nodes of linked lists.</button>
  <button type="button" class="oq-opt" data-i="1">It will print all the nodes of the linked list in reverse order.</button>
  <button type="button" class="oq-opt" data-i="2">It will print alternate nodes of the Linked List</button>
  <button type="button" class="oq-opt" data-i="3">It will print alternate nodes in reverse order</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="2" data-answers="1" hidden>
<h3>Q3. Consider the following code. What is the time complexity of this operation?

struct Node {
    int data;
    Node* next;
};

Node* reverseList(Node* head) {
    Node* prev = nullptr;
    Node* current = head;
    Node* next = nullptr;

    while (current != nullptr) {
        next = current-&gt;next;
        current-&gt;next = prev;
        prev = current;
        current = next;
    }
    head = prev;
    return head;
}</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">O(1)</button>
  <button type="button" class="oq-opt" data-i="1">O(n)</button>
  <button type="button" class="oq-opt" data-i="2">O(n log n)</button>
  <button type="button" class="oq-opt" data-i="3">O(n^2)</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="3" data-answers="3" hidden>
<h3>Q4. Given the following function to insert a node at the beginning of a doubly linked list, identify the missing line that correctly sets up the new node&#39;s backward link.

class Node {
public:
    int data;
    Node* next;
    Node* prev;
};

Node* outscal(Node* head, int newData) {
    Node* newNode = new Node();
    newNode-&gt;data = newData;
    newNode-&gt;next = head;
    newNode-&gt;prev = nullptr;
    if (head != nullptr)
        // Missing line
    head = newNode;
    return head;
}
</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">newNode-&gt;prev = head;</button>
  <button type="button" class="oq-opt" data-i="1">newNode-&gt;prev = newNode;</button>
  <button type="button" class="oq-opt" data-i="2">head-&gt;next = newNode;</button>
  <button type="button" class="oq-opt" data-i="3">head-&gt;prev = newNode;</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="4" data-answers="0" hidden>
<h3>Q5. What will be the output of the following code in a doubly linked list?

bool function(Node* head) {
    if (!head) return true;
    Node* left = head;
    Node* right = head;
    while (right-&gt;next) right = right-&gt;next;

    while (left != right &amp;&amp; left-&gt;prev != right) {
        if (left-&gt;data != right-&gt;data) return false;
        left = left-&gt;next;
        right = right-&gt;prev;
    }
    return true;
}


Given the doubly linked list: 1 &lt;-&gt; 2 &lt;-&gt; 3 &lt;-&gt; 2 &lt;-&gt; 1</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">True</button>
  <button type="button" class="oq-opt" data-i="1">False</button>
  <button type="button" class="oq-opt" data-i="2">Compilation error</button>
  <button type="button" class="oq-opt" data-i="3">Runtime error</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="5" data-answers="1" hidden>
<h3>Q6. Imagine your doubly linked list is initially: 1-2-3-4-5-6-7-8-9

Now, you perform the following operations in sequence:

Reverse the list.
Delete the middle node.
Insert a new node with value 10 at the end.
Reverse the list again.
Delete the head node.
Delete the middle node.

What would be the final list?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">9-8-7-6-5-4-3-2-1</button>
  <button type="button" class="oq-opt" data-i="1">1-2-3-6-7-8-9</button>
  <button type="button" class="oq-opt" data-i="2">1-2-3-4-5-6-7-8-10</button>
  <button type="button" class="oq-opt" data-i="3">1-2-3-4-5-10-6-7-8-9</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="6" data-answers="3" hidden>
<h3>Q7. Output of the following function to start pointing to the first node of the following linked list? 
1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;6

void fun(Node* start) {
    Node* current = start;
    while (current != nullptr) {
        cout &lt;&lt; current-&gt;data &lt;&lt; &quot; &quot;;
        current = current-&gt;next;
        if (current != nullptr) {
            current = current-&gt;next; 
        }
    }

    current = start;
    while (current != nullptr) {
        cout &lt;&lt; current-&gt;data &lt;&lt; &quot; &quot;;
        current = current-&gt;next;
        if (current != nullptr) {
            current = current-&gt;next; 
        }
    }
}</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">1 4 6 6 4 1</button>
  <button type="button" class="oq-opt" data-i="1">1 3 5 1 3 5</button>
  <button type="button" class="oq-opt" data-i="2">1 2 3 5</button>
  <button type="button" class="oq-opt" data-i="3">1 3 5 5 3 1</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="7" data-answers="1" hidden>
<h3>Q8. Then the function is called with the list containing the integers ( 1, 2, 3, 4, 5, 6, 7 ) in the given order. List contents after the complete function execution are:-

class Node {
public:
    int value;
    Node* next;
};

void rearrange(Node* list) {
    Node* p, *q;
    int temp;

    if (!list || !list-&gt;next) 
        return;

    p = list;
    q = list-&gt;next;

    while (q) {
        temp = p-&gt;value;
        p-&gt;value = q-&gt;value;
        q-&gt;value = temp;
        p = q-&gt;next;
        q = p ? p-&gt;next : nullptr;
    }
}</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">1,2,3,4,5,6,7</button>
  <button type="button" class="oq-opt" data-i="1">2,1,4,3,6,5,7</button>
  <button type="button" class="oq-opt" data-i="2">1,3,2,5,4,7,6</button>
  <button type="button" class="oq-opt" data-i="3">2,3,4,5,6,7,1</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="8" data-answers="3" hidden>
<h3>Q9. Which of the following applications makes use of a circular linked list?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Undo functionality in text editors</button>
  <button type="button" class="oq-opt" data-i="1">Dynamic memory allocation</button>
  <button type="button" class="oq-opt" data-i="2">Memory management algorithms</button>
  <button type="button" class="oq-opt" data-i="3">All of the above</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">9</span>.</p>
</div>
</div>
