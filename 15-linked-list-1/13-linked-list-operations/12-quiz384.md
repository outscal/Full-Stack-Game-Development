# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. What is the purpose of the "next" pointer in a singly linked list node?
- It points to the head node
- It points to the next node in the list **(correct)**
- It contains the data of the current node
- Not very important in Linked List

### Q2. Imagine a Singly Linked List representing a chain of characters in a text-based adventure game. How could you check if the list is empty (no characters)?
- Traverse the list and count nodes
- Check if the head pointer is NULL **(correct)**
- There's no way to check for emptiness

### Q3. What is the first step in inserting a node at the head of a linked list?
- Delete the head node
- Create a new node **(correct)**
- Update the head node to the node next to head node
- Traverse to the end of the list

### Q4. Why is the time complexity for insertion at the head O(1)?
- Because it involves only constant-time operations **(correct)**
- Because it requires traversing the entire list
- Because it uses a complex algorithm
- Because it depends on the number of nodes

### Q5. How does the new node's position relate to the reference node when inserting at the tail?
- The new node's position is the same as the reference node
- The new node's position is the forward position of the reference node
- The new node's position is the backward position of the reference node **(correct)**
- The new node's position is random

### Q6. What is the time complexity of finding the nth element from the end of a singly linked list and how many time would you traverse the list?
Given that you know the size of the linked list.
- O(n) **(correct)**
- O(log n)
- O(n^2)
- O(1)

### Q7. How many time would you traverse the list to find the nth element from the end?
Given that you know the size of the linked list.
- 1 **(correct)**
- 2
- 0
- 3

### Q8. To insert a new node at the beginning of a singly linked list, you need to:
- Update the next pointer of the new node to the current head.
- Update the head pointer to the new node and its next pointer to the old head. **(correct)**
- Traverse the list to find the tail and insert there.
- Create a new node with data and a null next pointer.

### Q9. A singly linked list can be converted to an array in time complexity of:
- O(1)
- O(log n)
- O(n) **(correct)**
- O(n^2)

### Q10. What will be the output of this program when executed?

#include <iostream>
using namespace std;

class Node {
public:
  int data;
  Node* next;
};

void outscal(Node* head) {
  while (head != NULL) {
    cout << head->data << " ";
    head = head->next; 
  }
}

int main() {
  Node* head = new Node();
  Node* second = new Node();
  Node* third = new Node();

  head->data = 1;
  head->next = second;

  second->data = 2;
  second->next = third;

  third->data = 3;
  third->next = NULL;

  outscal(head);
  outscal(head);

  return 0;
}
- 1 2 3
- 1 2 3 1 2 3 **(correct)**
- Undefined behavior
- Compile-time error

### Q11. Insertion of an element at the middle of a linked list requires the modification of how many pointers?
- 2 **(correct)**
- 1
- 3
- 4

### Q12. What should be checked before inserting a node at any position in a singly linked list?
- If the list is empty **(correct)**
- If the list has an even number of nodes
- If the list has an odd number of nodes
- If the list has exactly one node

### Q13. In a music playlist application, songs need to be inserted such that the most recently added song appears first in the list. Which insertion method would you use?
- Insert the new song at the tail
- Insert the new song at the head **(correct)**
- Insert the new song in the middle
- Insert the new song after the first song
