# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. How do you check if two linked lists are equal?
- By comparing their lengths
- By comparing each node one by one **(correct)**
- By comparing head node
- Two linked list can never be equal

### Q2. What is the purpose of a dummy node in a linked list?
- To mark the end of the list
- To mark the beginning of the list **(correct)**
- To avoid dealing with edge cases
- To improve memory efficiency

### Q3. In a game with a projectile system (e.g., shooting arrows), each projectile could be represented by a node in a Singly Linked List. What data might each node typically hold?
- Only the position (x,y) of the projectile
- Position (x,y), direction (angle), and a damage value
- Position (x,y), direction, a reference to the previous projectile, and a sprite image
- All of the above **(correct)**

### Q4. In a linked list with nodes A->B->C->D->E, what will be the linked list representation after half of the nodes are removed?
- A -> B -> C **(correct)**
- A -> B
- A->B->C->D

### Q5. Which of the following statements correctly describes the difference between reversing a linked list and reversing an array?
- Reversing a linked list can be done in O(1) time, while reversing an array takes O(n) time.
- Reversing a linked list involves changing pointers, whereas reversing an array involves swapping elements. **(correct)**
- Reversing an array is more space-efficient than reversing a linked list because it does not require additional memory allocation.
- Both reversing a linked list and an array have the same time complexity and require additional space for a new data structure.

### Q6. Explain the process of reversing a linked list using three pointers: cur_node, next_node, and prev_node.
- cur_node's next pointer is set to prev_node, then prev_node and cur_node are updated **(correct)**
- next_node's next pointer is set to prev_node, then prev_node and next_node are updated
- prev_node's next pointer is set to cur_node, then cur_node and prev_node are updated
- prev_node's next pointer is set to next_node, then next_node and prev_node are updated

### Q7. How would you handle scenarios where the snake reverses its direction while overlapping with its own body?
- Allow the snake to continue reversing until it exits the overlapping area
- Reverse the direction of the entire snake to prevent overlap **(correct)**
- Remove the overlapping body parts to resolve the conflict
- Pause the game and prompt the player to make a different move

### Q8. You are given a singly linked list with a head pointer. How can you check if the list is a palindrome (reads the same backward as forward)?

Eg: 1->2->2->1
- Use two pointers, one moving forward and one backward, comparing data at each step.
- Reverse the entire list and then compare it with the original list. **(correct)**
- Recursively traverse the list, checking for symmetry with the corresponding node from the end.
- Convert the list to a string, reverse the string, and compare it with the original string. **(correct)**
