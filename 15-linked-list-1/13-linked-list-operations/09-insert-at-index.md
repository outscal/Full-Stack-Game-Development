# **TASKS**


---



## INSERT NODE AT index


---



## IMPLEMENTING SHIFT NODES AFTER INSERTION


---

## [IN SINGLELINKEDLIST]



1. Create and Implement the `shiftNodesAfterInsertion()` to shift the nodes backwards to make space for the newly inserted node.





## IMPLEMENTING INSERT NODE AT INDEX


---

## [IN SINGLELINKEDLIST]



1. Create and Implement `insertNodeAtIndex()` to insert the new node at a specific index.
2. Check if the requested index is in the correct range
3. Use traversal to reach till that `index`, then insert the new node.
4. Initialize the new node.
5. Update the list structure by using `shiftNodesAfterInsertion()`.
6. Don't forget to increase the `linked_list_size` 



# Submission Instructions:


---



1. Commit and Push all your changes in the feature branch → *[*`**Feature-4-Linked-List-Operations**`*]*
2. Don't try to commit or merge your changes in the parent repo (the one you forked from) The Parent repo is also called the upstream repo.
3. So avoid pushing or merging commits in any upstream branches.
4. Once you are done with pushing all your commits for this assignment, create a PR from the `**Feature-4-Linked-List-Operations**` branch to the `**main**`
5. Submit the link of this PR as the submission link for this assignment.
