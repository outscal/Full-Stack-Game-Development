# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. What is a Linked List?
- A collection of elements where each element points to the next one, forming a sequential chain. **(correct)**
- A dynamic array that allows random access to any element.
- A type of data structure used to implement associative arrays.
- A fixed-size data structure that stores elements of the same type.

### Q2. In which scenario is a Linked List the best option?
- When you need fast access to elements at random positions.
- When you frequently need to add or remove elements from the middle of the list. **(correct)**
- When memory usage needs to be kept minimal.
- When the data needs to be stored on disk.

### Q3. Which one requires more memory: an integer array or a linked list with integer variables?
- Integer array, because it reserves space for all its elements in contiguous memory.
- Linked list, because it includes additional overhead for pointers along with the data. **(correct)**
- Both require the same amount of memory.
- It depends on the value of the integers.

### Q4. In an open-world RPG game, enemies are dynamically spawned and removed as the player explores different regions. This system must handle frequent changes, such as enemies appearing when entering new areas and disappearing after certain events, like defeating a boss.

In the context of managing dynamic enemy spawns in an open-world RPG, which data structure is most suitable?
- Using an array to manage enemies.
- Using a linked list to manage enemies. **(correct)**
- Using a fixed-size buffer to manage enemies.
- Using a hash table to manage enemies.

<details><summary>Explanation</summary>

A linked list allows efficient insertion and deletion of elements anywhere in the list without the need to shift other elements, making it ideal for managing dynamic enemy spawns.

</details>

### Q5. A high-performance particle system in an action game needs to manage tens of thousands of particles for effects like explosions and fire. Each particle's position, velocity, and state are updated every frame, requiring efficient access and updates.

For a particle system that requires efficient, per-frame updates of a large number of elements, which data structure should be avoided?
- Using a linked list to manage particles. **(correct)**
- Using a dynamic array to manage particles.
- Using a vector to manage particles.
- Using a double buffer to manage particles.

### Q6. In which of these game development scenarios should a Linked List be avoided due to its performance characteristics?
- Real-time updating of game scores displayed on a leaderboard.
- Implementing a save system that allows for frequent addition and removal of game states.
- Managing active quests where tasks can be added or removed dynamically.
- Rendering a sequence of frames for a smooth animation. **(correct)**
