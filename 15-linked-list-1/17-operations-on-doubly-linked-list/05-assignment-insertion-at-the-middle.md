# **Tasks**


---



## Implementing SHIFT NODES AFTER insertion


---

## [IN DoubleLinkedList]



1. Implement the `shiftNodesAfterInsertion()` to shift the nodes backwards to make space for the newly inserted node. 
2. Take reference from `SingleLinkedList`.





## Implementing insert NODE AT INDEX


---

## [IN DoubleLinkedList]



1. Implement `insertNodeAtIndex()` to insert the new node at a specific index. 
2. Check if the requested index is in the correct range
3. Initialize a new node
4. Insert the node at the desired index 
5. Shift the rest of the nodes using `shiftNodesAfterInsertion()` 
6. Increase the Linked List's size
7. Take reference from `SingleLinkedList`.



## Implementing insert NODE AT MIDDLE


---

## [IN DoubleLinkedList]



1. Implement the `insertNodeAtMiddle()` to insert a node at the middle index. 
2. Take reference from `SingleLinkedList`.





# Submission Instructions:


---



1. Commit and Push all your changes in the feature branch → *[*`**Feature-5-Doubly-Linked-List**`*]*
2. Don't try to commit or merge your changes in the parent repo (the one you forked from) The Parent repo is also called the upstream repo.
3. So avoid pushing or merging commits in any upstream branches.
4. Once you are done with pushing all your commits for this assignment, create a PR from the `**Feature-5-Doubly-Linked-List**` branch to the `**main**`
5. Submit the link of this PR as the submission link for this assignment.
