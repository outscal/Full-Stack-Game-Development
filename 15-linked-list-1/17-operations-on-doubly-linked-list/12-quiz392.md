# [⏳Quiz]

[⏳Quiz]


<div class="outscal-quiz" data-total="5">

<div class="oq-q" data-idx="0" data-answers="3,1">
<h3>Q1. Which of the following steps are unique to deleting a node at the tail of a DoubleLinkedList compared to a SingleLinkedList? Imagine you have a tail reference.</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Increasing the size of the list.</button>
  <button type="button" class="oq-opt" data-i="1">Traversing the list to the last node.</button>
  <button type="button" class="oq-opt" data-i="2">Setting the next pointer of the last node to the new node.</button>
  <button type="button" class="oq-opt" data-i="3">Setting the previous pointer of the new node to the last node.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="0" hidden>
<h3>Q2. In a doubly linked list, what is the time complexity of deleting a node given a pointer to that node?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">O(1)</button>
  <button type="button" class="oq-opt" data-i="1">O(logn)</button>
  <button type="button" class="oq-opt" data-i="2">O(n)</button>
  <button type="button" class="oq-opt" data-i="3">O(nlogn)</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="2" data-answers="3" hidden>
<h3>Q3. When deleting a node from a doubly linked list, what would happen if you forget to update the next node&#39;s previous pointer?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">The list will remain unchanged.</button>
  <button type="button" class="oq-opt" data-i="1">The list will crash immediately.</button>
  <button type="button" class="oq-opt" data-i="2">The previous node will not point correctly to the next node.</button>
  <button type="button" class="oq-opt" data-i="3"> The next node will not correctly point back to the previous node.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="3" data-answers="3" hidden>
<h3>Q4. What is the functionality of the following piece of code?

int function() {
    Node* temp = tail-&gt;getPrev();
    tail-&gt;setPrev(temp-&gt;getPrev());
    temp-&gt;getPrev()-&gt;setNext(tail);
    size--;
    return temp-&gt;getItem();
}
</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Return the element at the tail of the list but do not remove it</button>
  <button type="button" class="oq-opt" data-i="1">Return the element at the tail of the list and remove it from the list</button>
  <button type="button" class="oq-opt" data-i="2">Return the last but one element from the list but do not remove it</button>
  <button type="button" class="oq-opt" data-i="3">Return the last but one element at the tail of the list and remove it from the list</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="4" data-answers="0" hidden>
<h3>Q5. Which of the following code snippets correctly deletes a node from the middle of a doubly linked list given a pointer to that node?

A. node-&gt;prev-&gt;next = node-&gt;next;
node-&gt;next-&gt;prev = node-&gt;prev;
delete node;

B.node-&gt;next-&gt;prev = node;
node-&gt;prev-&gt;next = node-&gt;prev;
delete node;

C.node-&gt;prev-&gt;next = node-&gt;prev;
node-&gt;next-&gt;prev = node-&gt;next;
delete node;

D.node-&gt;next = node-&gt;prev;
node-&gt;prev = node-&gt;next;
delete node;</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">A</button>
  <button type="button" class="oq-opt" data-i="1">B</button>
  <button type="button" class="oq-opt" data-i="2">C</button>
  <button type="button" class="oq-opt" data-i="3">D</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">5</span>.</p>
</div>
</div>
