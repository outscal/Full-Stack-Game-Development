# [⏳Quiz]

[⏳Quiz]


<div class="outscal-quiz" data-total="6">

<div class="oq-q" data-idx="0" data-answers="0">
<h3>Q1. How do you split a doubly linked list into two halves?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">By traversing the list and splitting at the midpoint</button>
  <button type="button" class="oq-opt" data-i="1">By splitting at every alternate node</button>
  <button type="button" class="oq-opt" data-i="2">By randomly dividing the nodes</button>
  <button type="button" class="oq-opt" data-i="3">By reversing the second half</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="1" hidden>
<h3>Q2. What does the following function do in a doubly linked list?

void function(Node* head_ref, Node* temp) {
    if (head_ref == nullptr || temp == nullptr) return;
    if (head_ref == temp) *head_ref = temp-&gt;next;
    if (temp-&gt;next != nullptr) temp-&gt;next-&gt;prev = temp-&gt;prev;
    if (temp-&gt;prev != nullptr) temp-&gt;prev-&gt;next = temp-&gt;next;
    delete temp;
}</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Inserts a new node at a given position</button>
  <button type="button" class="oq-opt" data-i="1">Deletes a given node from the list</button>
  <button type="button" class="oq-opt" data-i="2">Reverses the list</button>
  <button type="button" class="oq-opt" data-i="3">Deletes head node from the list</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="2" data-answers="2" hidden>
<h3>Q3. You are given a doubly linked list with a head and tail pointer. How can you check if the list is a palindrome (reads the same backward as forward)?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0"> Traverse the list from both ends towards the middle and compare each element.</button>
  <button type="button" class="oq-opt" data-i="1">Reverse the list and compare it with the original list.</button>
  <button type="button" class="oq-opt" data-i="2">Compare the elements starting from the head and tail pointers simultaneously until they meet at the middle.</button>
  <button type="button" class="oq-opt" data-i="3">Check if the head and tail pointers point to the same node.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="3" data-answers="0" hidden>
<h3>Q4. Consider the following doubly linked list: head-1-2-3-4-5-tail. What will be the list after performing the given sequence of operations?

// some code
struct Node {
    int data;
    Node* next;
    Node* prev;

    Node(int data, Node* prev = nullptr, Node* next = nullptr) 
        : data(data), prev(prev), next(next) {}
};
// some code

Node temp = new Node(6,head,head.getNext());
head.setNext(temp);
temp.getNext().setPrev(temp);
Node temp1 = tail.getPrev();
tail.setPrev(temp1.getPrev());
temp1.getPrev().setNext(tail);

// some code</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">head-6-1-2-3-4-5-tail</button>
  <button type="button" class="oq-opt" data-i="1">head-6-1-2-3-4-tail</button>
  <button type="button" class="oq-opt" data-i="2">head-1-2-3-4-5-6-tail</button>
  <button type="button" class="oq-opt" data-i="3">head-1-2-3-4-5-tail</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="4" data-answers="1" hidden>
<h3>Q5. What was the purpose of the previous code snippet in a doubly linked list?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Inserting a new node with value 6 at the beginning of the list.</button>
  <button type="button" class="oq-opt" data-i="1">Inserting a new node with value 6 at the end of the list.</button>
  <button type="button" class="oq-opt" data-i="2">Deleting the last node from the list.</button>
  <button type="button" class="oq-opt" data-i="3">Reversing the list.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="5" data-answers="3" hidden>
<h3>Q6. Imagine your doubly linked list is initially: 1-2-3-4-5-6-7-8-9

Now, you perform the following operations in sequence:

1. Reverse the list.
2. Delete the head node.
3. Delete the middle node.
4. Reverse the list again.
5. Insert a new node with value 23 in the middle.
6. Delete the head node again.
7. Delete the middle node again.

What would be the final list?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">9-8-7-6-5-23-4</button>
  <button type="button" class="oq-opt" data-i="1">2-3-4-6-7-8</button>
  <button type="button" class="oq-opt" data-i="2">1-2-3-5-6-7-8-9</button>
  <button type="button" class="oq-opt" data-i="3">2-3-23-6-7-8</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">6</span>.</p>
</div>
</div>
