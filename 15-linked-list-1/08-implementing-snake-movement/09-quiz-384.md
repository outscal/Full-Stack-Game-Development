# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. Like in screen wrapping, where else can the modulus operation be used effectively in game development?
- To limit the frame rate of the game to ensure it doesn't exceed a certain threshold.
- To cycle through a list of items or effects that repeat after a certain number. **(correct)**
- To calculate the damage done by a player after hitting an enemy.
- To generate random terrain features in an endless runner game.

### Q2. If you had to create a snake that moves in the direction of the mouse cursor, what would be the most robust approach to implement it?
- Use an event listener to detect the mouse position and set the snake's direction towards it.
- Update the snake's position by subtracting the mouse's x and y coordinates from the snake's head.
- Continuously reset the game's origin point to the mouse cursor's position.
- Calculate the angle between the snake's head and the mouse cursor, and rotate the snake accordingly. **(correct)**

### Q3. While going left, why do you have to subtract 1 in the following function?

sf::Vector2i BodyPart::getNextPositionLeft()
{
  return sf::Vector2i((grid_position.x - 1 + LevelModel::number_of_columns) % (LevelModel::number_of_columns), grid_position.y);
}
- Subtracting 1 represents a movement to the left on a grid, decreasing the x-coordinate. **(correct)**
- The subtraction adjusts the snake's speed to be compatible with the game's framerate.
- It ensures that the snake's movement does not overlap with the game's UI elements.
- The subtraction is a placeholder for future expansions in game mechanics.

### Q4. Consider the following code snippet:

#include <iostream>
using namespace std;

int main() {
    int x = 11;
    int y = 4;
    cout << (x % y) << endl;
    return 0;
}

What will be the output of the program?
- 2
- 3 **(correct)**
- 1
- 4

### Q5. In a circular buffer implementation, how can the modulus operator (%) help manage the buffer indices?
- It helps calculate the average of the buffer values.
- It makes sure the index goes back to the start of the buffer when it reaches the end. **(correct)**
- It helps find the maximum value in the buffer.
- It sorts the buffer elements in ascending order.
