# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. Which of the following Design and Pseudo Code pairs will create a challenging food spawning algorithm in a snake game?

A) Design: Spawn food far from the snake to increase the difficulty of reaching it.
     Pseudo Code:
    function spawnFood():
    do
        position = getRandomPosition()
    while distance(position, snakeHead) < minimumDistance
    placeFood(position)

B) Design: Spawn food randomly without considering the snake's position.
    Pseudo Code:
    function spawnFood():
    position = getRandomPosition()
    placeFood(position)

C) Design: Spawn food close to obstacles to make it risky to retrieve.
     Pseudo Code:
     function spawnFood():
    do
        position = getRandomPositionNearObstacle(obstacles)
    while isSafe(position)
    placeFood(position)

D) Design: Spawn food on the body parts of the snake to increase the frequency of eating.
     Pseudo Code:
     function spawnFood():
    position = getRandomPositionOnSnake(snakeBody)
    placeFood(position)
- A
- B
- C **(correct)**
- D

### Q2. To increase the difficulty level progressively as the snake consumes more food in a game, which Design and Pseudo Code pair implements the best challenging food spawning algorithm?

A) Design: Spawn food at increasing distances from the snake.
Pseudo Code:
food_counter += 1
if food_counter % 5 == 0:
    distance_from_snake--
spawn_food(distance_from_snake)


B) Design: Spawn food randomly without considering the snake's consumption.
Pseudo Code:
spawn_food_at_random_location()


C) Design: Spawn food at increasingly difficult locations based on the number of food items eaten.
Pseudo Code:
if food_eaten % 5 == 0:
    spawn_food(far_from_snake())
else:
    spawn_food(random_location())


D) Design: Increase the rate of food item spawns as more food is consumed.
Pseudo Code:
decrease_spawn_time = max_base_time - (food_eaten * decrease_factor)
set_food_spawn_timer(decrease_spawn_time)

- A
- B
- C
- D **(correct)**
