# [⏳Quiz]

[⏳Quiz]


<div class="outscal-quiz" data-total="2">

<div class="oq-q" data-idx="0" data-answers="2">
<h3>Q1. Which of the following Design and Pseudo Code pairs will create a challenging food spawning algorithm in a snake game?

A) Design: Spawn food far from the snake to increase the difficulty of reaching it.
     Pseudo Code:
    function spawnFood():
    do
        position = getRandomPosition()
    while distance(position, snakeHead) &lt; minimumDistance
    placeFood(position)

B) Design: Spawn food randomly without considering the snake&#39;s position.
    Pseudo Code:
    function spawnFood():
    position = getRandomPosition()
    placeFood(position)

C) Design: Spawn food close to obstacles to make it risky to retrieve.
     Pseudo Code:
     function spawnFood():
    do
        position = getRandomPositionNearObstacle(obstacles)
    while isSafe(position)
    placeFood(position)

D) Design: Spawn food on the body parts of the snake to increase the frequency of eating.
     Pseudo Code:
     function spawnFood():
    position = getRandomPositionOnSnake(snakeBody)
    placeFood(position)</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">A</button>
  <button type="button" class="oq-opt" data-i="1">B</button>
  <button type="button" class="oq-opt" data-i="2">C</button>
  <button type="button" class="oq-opt" data-i="3">D</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="3" hidden>
<h3>Q2. To increase the difficulty level progressively as the snake consumes more food in a game, which Design and Pseudo Code pair implements the best challenging food spawning algorithm?

A) Design: Spawn food at increasing distances from the snake.
Pseudo Code:
food_counter += 1
if food_counter % 5 == 0:
    distance_from_snake--
spawn_food(distance_from_snake)


B) Design: Spawn food randomly without considering the snake&#39;s consumption.
Pseudo Code:
spawn_food_at_random_location()


C) Design: Spawn food at increasingly difficult locations based on the number of food items eaten.
Pseudo Code:
if food_eaten % 5 == 0:
    spawn_food(far_from_snake())
else:
    spawn_food(random_location())


D) Design: Increase the rate of food item spawns as more food is consumed.
Pseudo Code:
decrease_spawn_time = max_base_time - (food_eaten * decrease_factor)
set_food_spawn_timer(decrease_spawn_time)
</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">A</button>
  <button type="button" class="oq-opt" data-i="1">B</button>
  <button type="button" class="oq-opt" data-i="2">C</button>
  <button type="button" class="oq-opt" data-i="3">D</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">2</span>.</p>
</div>
</div>
