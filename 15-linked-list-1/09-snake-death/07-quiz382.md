# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. How would you modify the collision detection mechanism to account for a new 'ghost mode' feature where the snake can pass through its body parts for a temporary duration?
- Create a separate function for node collision detection during ghost mode  
- Add a condition in processNodeCollision() to bypass collision checks if ghost mode is active. **(correct)**
- Remove all body parts except the head during ghost mode.
- Increase the speed of the snake so it passes through itself without collisions.

### Q2. Considering the function SingleLinkedList::processNodeCollision(), why might it be necessary to exclude the head node from the collision check loop?
- The head node does not have a preceding body part to collide with.
- Since the current code compares position to detect collision, head node will always collide with itself **(correct)**
- The head node is always immune to collisions according to the game rules.
- Collision checks are only relevant for tail nodes in optimized game algorithms.

### Q3. How should you delete the snake on death?
- Set the head node to nullptr and the tail node to nullptr.
- Iterate through the linked list, setting each node's data to 0, and then set the head node to nullptr.
- Continuously remove the head node and delete it until the head node becomes nullptr. **(correct)**
- Set the head node to head_node->next_node and the tail node to head_node.
