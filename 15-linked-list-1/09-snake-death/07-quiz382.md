# [⏳Quiz]

[⏳Quiz]


<div class="outscal-quiz" data-total="3">

<div class="oq-q" data-idx="0" data-answers="1">
<h3>Q1. How would you modify the collision detection mechanism to account for a new &#39;ghost mode&#39; feature where the snake can pass through its body parts for a temporary duration?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Create a separate function for node collision detection during ghost mode  </button>
  <button type="button" class="oq-opt" data-i="1">Add a condition in processNodeCollision() to bypass collision checks if ghost mode is active.</button>
  <button type="button" class="oq-opt" data-i="2">Remove all body parts except the head during ghost mode.</button>
  <button type="button" class="oq-opt" data-i="3">Increase the speed of the snake so it passes through itself without collisions.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="1" hidden>
<h3>Q2. Considering the function SingleLinkedList::processNodeCollision(), why might it be necessary to exclude the head node from the collision check loop?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">The head node does not have a preceding body part to collide with.</button>
  <button type="button" class="oq-opt" data-i="1">Since the current code compares position to detect collision, head node will always collide with itself</button>
  <button type="button" class="oq-opt" data-i="2">The head node is always immune to collisions according to the game rules.</button>
  <button type="button" class="oq-opt" data-i="3">Collision checks are only relevant for tail nodes in optimized game algorithms.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="2" data-answers="2" hidden>
<h3>Q3. How should you delete the snake on death?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Set the head node to nullptr and the tail node to nullptr.</button>
  <button type="button" class="oq-opt" data-i="1">Iterate through the linked list, setting each node&#39;s data to 0, and then set the head node to nullptr.</button>
  <button type="button" class="oq-opt" data-i="2">Continuously remove the head node and delete it until the head node becomes nullptr.</button>
  <button type="button" class="oq-opt" data-i="3">Set the head node to head_node-&gt;next_node and the tail node to head_node.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">3</span>.</p>
</div>
</div>
