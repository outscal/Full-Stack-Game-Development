# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. What would be the time complexity of the following code?

void connectNodes(Node* first, Node* second) {
    first->next = second;
}
- O(1) **(correct)**
- O(log n)
- O(n)
- None of the above

### Q2. Given the following code snippet:

void function(Node* head, int data) {
    
    if (head == NULL) {
        head = data;
        return;
    }
    
    Node* temp = head;
    while (temp->next != NULL) {
        temp = temp->next;
    }
    temp->next = data;
}

Assuming the initial linked list is 2 -> 4 -> 6 . What will be the resulting linked list?
- 2 -> 4 -> 6 -> 8
- 2 -> 4 -> 6
- 2 -> 4 -> 6 -> NULL
- None of the above **(correct)**

### Q3. What will be the output of the following code?

void printList(Node* head) {
    Node* temp = head;
    while (temp->next->next != NULL) {
        printf("%d ", temp->data);
        temp = temp->next;
    }
}

Assuming the linked list is 3 -> 1 -> 4 -> 1 -> 5 .
- 3 1 4 1 5
- 3 1 4 1
- 3 1 4 **(correct)**
- 1 4 1 5

### Q4. What is the purpose of a head pointer in a linked list?
- It points to the last node of the list
- It points to the middle node of the list
- It points to the first node of the list **(correct)**
- It is unused in linked lists

### Q5. In the context of a real-time gaming application, why might a linked list be less optimal than an array for managing dynamic game objects?
- Linked lists provide slower access times due to non-contiguous memory allocation, which can hinder performance where fast access is crucial. **(correct)**
- Game objects in linked lists are less secure and can be easily corrupted by external processes.
- Linked lists consume more CPU resources due to the complex algorithms required to manage them.
- The graphical rendering of objects in a linked list is less efficient compared to those stored in an array.

### Q6. In a single linked list used for undo functionality in a text editor, what is the primary challenge when implementing an undo operation?
- Determining which node corresponds to a particular state without timestamps.
- Quickly reverting to the previous state without traversing from the head. **(correct)**
- Maintaining a fixed size of the linked list to conserve memory.
- Ensuring that all nodes are visible to the user for selection.

### Q7. How can linked list traversal be optimized if the linked list nodes are accessed frequently in a specific pattern?
- By restructuring the linked list into a circular list to reduce traversal time.
- By indexing nodes and storing them in an auxiliary array for quick access.
- By caching frequently accessed nodes in a separate data structure. **(correct)**
- By converting the linked list to a binary search tree for faster lookup.

### Q8. Why must you copy the head node into a temporary node for traversing?
- To avoid losing the head node, which is critical for maintaining the start of the list. **(correct)**
- To increase the speed of traversal by using a lighter copy of the node.
- To be able to reverse the list after reaching the end.
- To check the integrity of the list by comparing the original and copied nodes.

### Q9. Explain the logic for the Linked List traversing code:

cur_node = head_node;     
while (cur_node->next != nullptr) {   
       cur_node = cur_node->next;
}

- The code continuously moves to the next node, starting from the head until it reaches the last node. **(correct)**
- The code resets each node’s next pointer to null to clear the list.
- The code checks if the current node is null and stops if it is.
- The code duplicates each node to create a backup of the linked list.

### Q10. What is the time complexity of inserting a new node at the tail in a Linked List?
- O(1), as the node is added directly at the end.
- O(log n), as it involves a binary search to find the tail.
- O(n), as it requires traversal from the head to the tail to find the last node. **(correct)**
- O(n^2), due to repeated operations for each node addition.

### Q11. Why are you inserting Body Parts at the tail while initializing?
- To ensure the snake grows from the front, making it appear that it is moving forward.
- Inserting nodes at the head would shift the starting position of the head during the game, creating confusion for the game designer regarding the initial spawning location of the snake. **(correct)**
- Adding nodes at the tail helps enhance the graphics rendering speed by maintaining a fixed origin point.
- To simplify the deletion of nodes from the front.
