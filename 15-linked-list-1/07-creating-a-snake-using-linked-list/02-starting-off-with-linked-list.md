# **TASKS**


---



## CreatE single linked list


---

1. Create Empty `SingleLinkedList` class with empty lifecycle methods other than `initialize()` and `update()` and follow `namespace LinkedList` 
2. Move the `Node.h` file in `LinkedList` folder and create nested `namespace` inside it.



## [IN SingleLinkedList]

1. Create the following properties:
2. 1. `Node* head_node`
  2. `float node_width`
  3. `float node_height`
  4. `sf::Vector2i default_position`
  5. `Direction default_direction`
3. Set `head_node` as `nullptr` in constructor.
4. Create and impelement `void initialize(float width, float height, sf::Vector2i position, Direction direction)` 
5. Create and implement a `private` method `Node* createNode()` and return the reference for new `Node`.

# Submission Instructions:


---



1. Commit and Push all your changes in the feature branch → *[*`**Feature-2-Linked-List**`*]*
2. Don't try to commit or merge your changes in the parent repo (the one you forked from) The Parent repo is also called the upstream repo.
3. So avoid pushing or merging commits in any upstream branches.
4. Once you are done with pushing all your commits for this assignment, create a PR from the `**Feature-2-Linked-List**` branch to the `**main**`
5. Submit the link of this PR as the submission link for this assignment.
