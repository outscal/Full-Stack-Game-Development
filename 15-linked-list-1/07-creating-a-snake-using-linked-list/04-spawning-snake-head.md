# **TASKS**


---



## spawn snake head


---



## [IN SingleLinkedList]

1. Create a method `createHeadNode()` and initialize `head_node`



## [IN **SnakeController**]

1. Add a reference of `SingleLinkedList* single_linked_list` and set it as `nullptr` in constructor.
2. Create a method `void createLinkedList()` that create a new object of `SingleLinkedList` and set it to `single_linked_list`.
3. Call `SingleLinkedList`'s `initialize()` of inside `initialize()` passing all necessary parameters.
4. Call `SingleLinkedList`'s `createHeadNode()` in `spawnSnake()`.
5. Call  `SingleLinkedList`'s `render()` in `render()` 

# Submission Instructions:


---



1. Commit and Push all your changes in the feature branch → *[*`**Feature-2-Linked-List**`*]*
2. Don't try to commit or merge your changes in the parent repo (the one you forked from) The Parent repo is also called the upstream repo.
3. So avoid pushing or merging commits in any upstream branches.
4. Once you are done with pushing all your commits for this assignment, create a PR from the `**Feature-2-Linked-List**` branch to the `**main**`
5. Submit the link of this PR as the submission link for this assignment.
