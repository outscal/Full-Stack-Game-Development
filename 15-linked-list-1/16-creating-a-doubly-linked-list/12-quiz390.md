# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. When comparing the memory usage of singly and doubly linked lists, which statement is generally true?
- Singly linked lists use less memory as they have one pointer per node. **(correct)**
- Doubly linked lists use less memory due to more efficient node packing.
- The memory usage is roughly the same for both types of linked lists.
- It depends on the specific implementation and data type stored in the nodes.

### Q2. What is the primary advantage of creating a base Node class in a linked list implementation?
- It allows all nodes to share common functionalities and attributes. **(correct)**
- It ensures each node type (single, double) has unique functionalities.
- It simplifies the creation of new node types by not requiring inheritance.
- It eliminates the need for separate linked list classes.

### Q3. Which of the following statements about the LinkedList class is incorrect?
- It contains a method to reverse the linked list.
- It has pure virtual functions that must be overridden in derived classes.
- It implements all the functionalities required by singly and doubly linked lists. **(correct)**
- It includes a function to detect collisions among nodes.

### Q4. Why is the virtual keyword used in some functions within the LinkedList class?
- To allow the functions to be overridden in derived classes. **(correct)**
- To prevent the functions from being overridden.
- To make the class abstract.
- To enable multiple inheritance.

### Q5. In the LinkedList class, why are some functions declared as pure virtual?
- To force derived classes to provide specific implementations. **(correct)**
- To provide default implementations that derived classes can override.
- To prevent derived classes from modifying these functions.
- To make the base class instantiable.

### Q6. What is the significance of marking the destructor as virtual in the LinkedList class?
- It prevents the class from being instantiated.
- It enables multiple inheritance.
- It allows derived classes to clean up resources properly. **(correct)**
- It provides a default implementation for the destructor.
