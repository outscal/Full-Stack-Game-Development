# [⏳Quiz]

[⏳Quiz]


<div class="outscal-quiz" data-total="3">

<div class="oq-q" data-idx="0" data-answers="1">
<h3>Q1. what will be the output of the code?

#include&lt;iostream&gt;
using namespace std;

void fun(){
     static int x = 5;
     int a = 6;
      x++;
      a++;
     cout &lt;&lt; a &lt;&lt;&quot; &quot; &lt;&lt; x &lt;&lt; &quot; &quot;;
}

int main(){
   fun();
   fun();
   fun();
   
   return 0;
}</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">7 6 7 6 7 6</button>
  <button type="button" class="oq-opt" data-i="1">7 6 7 7 7 8</button>
  <button type="button" class="oq-opt" data-i="2">7 6 7 7 8 8</button>
  <button type="button" class="oq-opt" data-i="3">6 5 6 5 6 5</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="3" hidden>
<h3>Q2. Which of the following statements about static variables in C++ is false?
</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">A static variable inside a function retains its value between function calls.</button>
  <button type="button" class="oq-opt" data-i="1">A static variable inside a function is initialized only once, and the initialization happens during the first function call.</button>
  <button type="button" class="oq-opt" data-i="2">A static member variable belongs to the class itself and is shared by all instances of the class.</button>
  <button type="button" class="oq-opt" data-i="3">A static local variable&#39;s scope is global, and it can be accessed outside of the function using an external linkage.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="2" data-answers="0" hidden>
<h3>Q3. What will be the output of the code?

#include&lt;iostream&gt;
using namespace std;

class Test {
public:
    Test() { count++; }
    ~Test() { count--; }

    static int getCount() {
        return count;
    }

private:
    static int count;
};

int Test::count = 0;

int main() {
    Test t1, t2;
    cout &lt;&lt; Test::getCount() &lt;&lt; &quot; &quot;;  // First output
    {
        Test t3;
        cout &lt;&lt; Test::getCount() &lt;&lt; &quot; &quot;;  // Second output
    }
    cout &lt;&lt; Test::getCount() &lt;&lt; &quot; &quot;;  // Third output
    return 0;
}

</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">2 3 2</button>
  <button type="button" class="oq-opt" data-i="1">3 2 3</button>
  <button type="button" class="oq-opt" data-i="2">2 2 1</button>
  <button type="button" class="oq-opt" data-i="3">1 2 1</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">3</span>.</p>
</div>
</div>
