# [⏳Quiz]

[⏳Quiz]


<div class="outscal-quiz" data-total="3">

<div class="oq-q" data-idx="0" data-answers="3">
<h3>Q1. You have a class A that defines a method display(). Both class B and class C inherit from A and override the display() method. Class D inherits from both B and C. If you create an object of class D and call D.display(), which version of display() will be executed in a language that does not support virtual inheritance?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">The display() method from class B</button>
  <button type="button" class="oq-opt" data-i="1">The display() method from class C</button>
  <button type="button" class="oq-opt" data-i="2">The display() method from class A</button>
  <button type="button" class="oq-opt" data-i="3">This will cause a compilation error due to ambiguity</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="2" hidden>
<h3>Q2. What is the primary purpose of using virtual inheritance in languages like C++ when dealing with the diamond problem?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0"> To prevent the child class from inheriting members from its parent classes</button>
  <button type="button" class="oq-opt" data-i="1">To allow multiple inheritance without introducing method ambiguity</button>
  <button type="button" class="oq-opt" data-i="2">To ensure that a base class is only inherited once, even when inherited through multiple paths</button>
  <button type="button" class="oq-opt" data-i="3"> To avoid memory allocation issues during inheritance</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="2" data-answers="2" hidden>
<h3>Q3. What will be the output of the given code?

class Pokemon {
public:
    void attack() {
        std::cout &lt;&lt; &quot;Pokemon uses a basic attack!&quot;;
    }
};

class FireType : public virtual Pokemon {
public:
    void attack() {
        std::cout &lt;&lt; &quot;FireType Pokemon uses Fire Attack!&quot;;
    }
};

class FlyingType : public virtual Pokemon {
public:
    void attack() {
        std::cout &lt;&lt; &quot;FlyingType Pokemon uses Flying Attack!&quot;;
    }
};

class Charizard : public FireType, public FlyingType {};

int main() {
    Charizard charizard;
    charizard.attack();
    return 0;
}
</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0"> &quot;FireType Pokemon uses Fire Attack!&quot;</button>
  <button type="button" class="oq-opt" data-i="1"> &quot;FlyingType Pokemon uses Flying Attack!&quot;</button>
  <button type="button" class="oq-opt" data-i="2">Compilation error due to ambiguous attack() method</button>
  <button type="button" class="oq-opt" data-i="3">&quot;Pokemon uses a basic attack!&quot;</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">3</span>.</p>
</div>
</div>
