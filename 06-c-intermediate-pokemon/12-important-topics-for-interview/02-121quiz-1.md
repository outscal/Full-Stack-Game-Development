# [⏳Quiz]

[⏳Quiz]


<div class="outscal-quiz" data-total="3">

<div class="oq-q" data-idx="0" data-answers="0">
<h3>Q1. Consider the following code:

class Base {
public:
    int publicVar;
protected:
    int protectedVar;
private:
    int privateVar;
};

class Derived : protected Base {
public:
    void display() {
        publicVar = 10;
        protectedVar = 20;
        // privateVar = 30; // Error
    }
};

int main() {
    Derived d;
    d.publicVar = 10;  // Error
    return 0;
}</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0"> Because publicVar becomes protected in the Derived class</button>
  <button type="button" class="oq-opt" data-i="1">Because publicVar becomes private in the Derived class</button>
  <button type="button" class="oq-opt" data-i="2">Because publicVar is private in the Base class</button>
  <button type="button" class="oq-opt" data-i="3"> Because publicVar is inaccessible due to private inheritance</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="1" hidden>
<h3>Q2. When a class is inherited using the public access specifier in C++, how are the access levels of the base class members affected in the derived class?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">All members of the base class become public in the derived class</button>
  <button type="button" class="oq-opt" data-i="1">public members remain public, protected members remain protected, and private members remain inaccessible</button>
  <button type="button" class="oq-opt" data-i="2">All members of the base class become protected in the derived class</button>
  <button type="button" class="oq-opt" data-i="3"> All members of the base class become private in the derived class</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="2" data-answers="1" hidden>
<h3>Q3. What will happen in the following code if the inheritance specifier for Derived is changed from public to private?


class Base {
public:
    void display() {
        std::cout &lt;&lt; &quot;Base class display&quot; &lt;&lt; std::endl;
    }
};

class Derived : public Base {
public:
    void show() {
        std::cout &lt;&lt; &quot;Derived class show&quot; &lt;&lt; std::endl;
    }
};

int main() {
    Derived d;
    d.display();  // Line 1
    d.show();     // Line 2
    return 0;
}
</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0"> Both d.display() and d.show() will still work correctly</button>
  <button type="button" class="oq-opt" data-i="1">Only d.show() will work, and d.display() will cause a compilation error</button>
  <button type="button" class="oq-opt" data-i="2"> Both d.display() and d.show() will cause compilation errors</button>
  <button type="button" class="oq-opt" data-i="3">Only d.display() will work, but d.show() will cause an error</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">3</span>.</p>
</div>
</div>
