# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. Which of the following operators cannot be overloaded as a member function, but can be overloaded as a friend function?


- +
- [ ]
- =
- << **(correct)**

### Q2. Which of the following statements is true?

class Test {
public:
    Test operator++(int);  // Post-increment
};


- This overload will be used for both pre- and post-increment operations.
- This overload handles the pre-increment operation.
-  This overload handles the post-increment operation and returns the object before the increment. **(correct)**
- This overload handles the post-increment operation and returns the object after the increment.

### Q3. When overloading the assignment operator (=) for a class in C++, which of the following conditions should be handled to avoid issues with self-assignment?


-  Check for nullptr before assignment
-  Check if the addresses of the left-hand and right-hand sides are the same **(correct)**
- Perform deep copying before checking self-assignment
- Return a new object by value from the assignment operator

### Q4. What will be the output of the code?

#include <iostream>
using namespace std;

class Complex {
public:
    int real, imag;

    Complex(int r = 0, int i = 0) : real(r), imag(i) {}

    Complex operator+(const Complex& obj) {
        return Complex(real + obj.real, imag + obj.imag);
    }

    void display() {
        cout << real << " + i" << imag << endl;
    }
};

int main() {
    Complex c1(3, 5), c2(1, 2);
    Complex c3;
    c3 = c1 + c2;
    c3.display();

    c3 = c1 + 5;  // Implicit conversion
    c3.display();

    return 0;
}


- 4 + i7      8 + i5
- 4 + i7     3 + i10
- 4 + i7     8 + i7 **(correct)**
- 4 + i7    3 + i7
