# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. In C++, what is the effect of passing a variable to a function using Call by Value?
- The function modifies the original variable.
- The function creates a copy of the variable and modifies the copy. **(correct)**
- The function passes a reference to the original variable.
- The function does not receive any value from the original variable.

### Q2. Which of the following is true about passing an array to a function in C++?
- Arrays are always passed by value.
- Arrays are always passed by reference.
- Arrays cannot be passed to functions.
- Arrays are passed by reference because the name of the array acts as a pointer. **(correct)**

### Q3. What happens when a large object is passed by value to a function in C++?
- The object is passed by reference automatically.
- A copy of the object is created, which can be inefficient. **(correct)**
- The function modifies the original object.
- The compiler throws an error.

### Q4. What will be the output of the following code snippet?

void increment(int &x) {
    x++;
}

int main() {
    int a = 1;
    increment(a++);
    cout << a;
    return 0;
}
- 1
- 2
- 3 **(correct)**
- Undefined Behaviour

### Q5. What will be the output of the following code snippet?

#include<bits/stdc++.h>
using namespace std;

int& getReference() {
    int x = 10;
    return x;
    
    x++;
}

int main() {
    int &a = getReference();
    cout << a;
    
    return 0;
}
- 10
- 11
- Compilation Error
- Undefined behaviour, because local variable reference is passed outside the function **(correct)**
