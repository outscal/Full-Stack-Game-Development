# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. Can a class in C++ have multiple constructors?
- Yes, but they must have different names.
- No, a class can have only one constructor.
- Yes, but they must have different parameter lists. **(correct)**
- Yes, but only one constructor can be defined as public.

### Q2. Which of the following statements about constructors and destructors is true?
- Constructors are called only when an object is created; destructors are called when an object is deleted. **(correct)**
- Destructors are called automatically when the program starts, and constructors are called when the program ends.
- Both constructors and destructors can be overloaded.
- Destructors can have parameters, while constructors cannot.

### Q3. What will be the output of the following code snippet?

#include<bits/stdc++.h>

class A {
public:
    A() { std::cout << "A's Constructor\n"; }
    ~A() { std::cout << "A's Destructor\n"; }
};

class B : public A {
public:
    B() { std::cout << "B's Constructor\n"; }
    ~B() { std::cout << "B's Destructor\n"; }
};

int main() {
    B obj;
    return 0;
}
- A's Constructor\nB's Constructor\nB's Destructor\nA's Destructor **(correct)**
- B's Constructor\nA's Constructor\nA's Destructor\nB's Destructor
- A's Constructor\nB's Constructor\nA's Destructor\nB's Destructor
- B's Constructor\nA's Constructor\nB's Destructor\nA's Destructor
