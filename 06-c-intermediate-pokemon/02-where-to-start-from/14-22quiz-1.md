# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. What are the extra benefits that Enum class provide compared to enum?
- Let you use the same value in multiple enums
- Give you a searching functionality
- Can define a variable with the same name as the value inside enum
- Both a and c **(correct)**

### Q2. What will be the output of the following code?

#include <iostream>
using namespace std;

enum Color { RED = 1, GREEN, BLUE = 5, YELLOW };

int main() {
    Color c1 = GREEN;
    Color c2 = YELLOW;

    cout << c1 << " " << c2 << endl;

    return 0;
}
- 1 5
- 2 6 **(correct)**
- 2 3
- 0 2

### Q3. Which of the line will throw an error?

#include<iostream>
using namespace std;

enum Color { Red, Green, Blue };
enum class Fruit { Apple, Orange, Banana };

int main() {
    Color x = Red;            // Line 1
    Fruit y = Fruit::Orange;  // Line 2
    Color c = Blue;           // Line 3
    Fruit f = Banana;         // Line 4
    
    return 0;
}
- Line 1
- Line 2
- Line 3
- Line 4 **(correct)**

### Q4. Which of the following will correctly initializes a variable?

1. 
enum class Direction { North, South, East, West };
Direction d = North;

2.
enum class Direction { North, South, East, West };
Direction d = Direction::North;

3.
enum class Direction { North, South, East, West };
Direction d = 0;

4. 
enum class Direction { North = 1, South = 2, East = 3, West = 4 };
Direction d = 1;
- 1
- 2 **(correct)**
- 3
- 4
