# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. What is the output of the following code?

#include<iostream>
using namespace std;

int main()
{
      int *ptr;
      int x = 5;
      cout << ptr << endl;
}


- 5 
-  address of  x
-  address of ptr 
- garbage value **(correct)**

### Q2. In previous Question, pointer ptr remains uninitialized and prints garbage memory address, such type of pointer is called
- Garbage pointer
- Null Pointer
- Wild Pointer **(correct)**
-  Void Pointer

### Q3. What is Null Pointer ?
- A pointer that points to an integer variable.
- A pointer that points the address of wild pointer.
-  A pointer that doesn't point to any valid memory location. **(correct)**
- A pointer that holds the address of previously allocated memory after it has been deleted.

### Q4. What will be the output of the code?

#include<iostream>
using namespace std;

int main(){

    int *ptr = NULL;
    cout << *(ptr) << endl;
    
   return 0;
}
- The program will output 0
- The program will output a garbage value.
- The program will run without output.
-  The program will likely crash with a segmentation fault or runtime error. **(correct)**

### Q5. What will be the output of the following code ?

#include<iostream>
using namespace std;

void function(int* ptr) {
    int x = 5;
    ptr = &x;
}

int main(){
   
   int *ptr;
   function(ptr);
     
   cout << *ptr << endl;
}
-  5
- address of  x .
-  garbage value
- segmentation fault **(correct)**

### Q6. In the Previous Question, Pointer ptr initially points to a valid memory location of x variable. but after the memory of  x  is freed, it points to an invalid or unreserved memory location, such type of pointer is called
- Null Pointer
- Void Pointer
- Dangling Pointer **(correct)**
- Wild Pointer
