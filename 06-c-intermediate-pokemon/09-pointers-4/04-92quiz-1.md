# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. Why are memory addresses represented in hexadecimal format?
- Hexadecimal is faster for computers to process than binary.
-  Hexadecimal provides a more compact and human-readable representation       of long binary numbers. **(correct)**
- Hexadecimal is the natural number system used by all programming languages.
-  Hexadecimal addresses consume less memory than binary addresses.

### Q2. How does the CPU know the value of a variable  a in a C++ program and print on output console?

#include<iostream>
using namespace std;

int main(){
   int a = 10;
   cout << a << endl;
}
- Value store in Hard disk and CPU’s take value from there.
- The variable holds the value in the CPU's cache memory.
- The compiler maps the variable to a specific memory address in RAM, and the CPU retrieves the value stored at that address when needed. **(correct)**
- The value of the variable is always pre-calculated and printed by the compiler.

### Q3. Integer variable have 4 bytes and each bytes have different memory address. Which one of the four byte address will store in ptr variable in below code :

#include<iostream>
using namespace std;

int main()
{
    int a = 10;
    int *ptr = &a;
    
    return 0;
}
- First Byte  **(correct)**
- Second byte
- Third byte
- Fourth Byte
