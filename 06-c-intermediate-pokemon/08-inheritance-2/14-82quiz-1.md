# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. Which show() method will be called when basePtr->show() is executed?

#include <iostream>
using namespace std;

class Base {
public:
    void show() {
        cout << "Base class show() method" << endl;
    }
};

class Derived : public Base {
public:
    void show() {
        cout << "Derived class show() method" << endl;
    }
};

int main() {
    Base* basePtr;
    Derived derivedObj;
    basePtr = &derivedObj;

    basePtr->show(); // Which show() will be called?

    return 0;
}

-  Base class show() method **(correct)**
- Derived class show() method
- Compiler error
-  Runtime error

### Q2. Which display() method will be called when childObj.display() is executed?

#include <iostream>
using namespace std;

class Parent {
public:
    void display() {
        cout << "Parent class display() method" << endl;
    }
};

class Child : public Parent {
public:
    void display(int x) {
        cout << "Child class display() method with argument: " << x << endl;
    }
};

int main() {
    Child childObj;
    childObj.display(); // Will this compile?
    
    return 0;
}

- Yes, the Child class display() method will be called.
- No, it will result in a compile-time error because the base class display() method is hidden. **(correct)**
- Yes, but the Parent class display() method will be called.
-  No, a runtime error will occur due to method hiding.

### Q3. Which greet() method is called in each case?

#include <iostream>
using namespace std;

class Grandparent {
public:
    void greet() {
        cout << "Hello from Grandparent!" << endl;
    }
};

class Parent : public Grandparent {
public:
    void greet() {
        cout << "Hello from Parent!" << endl;
    }
};

class Child : public Parent {
public:
    void greet() {
        cout << "Hello from Child!" << endl;
    }
};

int main() {
    Child childObj;
    childObj.greet(); // Which greet() method will be called?

    Parent* parentPtr = &childObj;
    parentPtr->greet(); // Which greet() method will be called now?

    Grandparent* grandparentPtr = &childObj;
    grandparentPtr->greet(); // Which greet() method will be called here?

    return 0;
}

When childObj.greet() is called?
- Grandparent's greet()
- Parent's greet()
- Child's greet() **(correct)**
- None of the above

### Q4. In a Previous Question, When parentPtr->greet() is called?
- Grandparent's greet()
- Parent's greet() **(correct)**
- Child's greet()
- None of the above

### Q5. In Previous Question, When grandparentPtr->greet() is called?
- Grandparent's greet() **(correct)**
- Parent's greet()
- Child's greet()
- None of the above
