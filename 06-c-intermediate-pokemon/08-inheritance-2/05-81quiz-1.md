# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. Which of the following access specifiers allows a derived class to access protected members of the base class?
- private
- protected **(correct)**
- public
- None of the above

### Q2. Which of the following is true about multiple inheritance in C++?
- A derived class can inherit from only one base class.
- A derived class can inherit from more than one base class. **(correct)**
- A derived class can only inherit from abstract base classes.
- C++ does not support multiple inheritance.

### Q3. What will happen if a class is inherited privately in C++?
- All the public and protected members of the base class become private in the derived class. **(correct)**
- All the public members of the base class become public in the derived class.
- All the private members of the base class become public in the derived class.
- The derived class cannot access any of the base class members.

### Q4. Which of the following code snippets will result in a compilation error?

1. 
class Base {
    int a;
};

class Derived : public Base {
    int b;
};

2.
class Base {
protected:
    int a;
};

class Derived : public Base {
    void show() { a = 5; }
};

3.
class Base {
public:
    int a;
};

class Derived : public Base {
    void show() { a = 5; }
};

4.
class Base {
private:
    int a;
};

class Derived : public Base {
    void show() { a = 5; }
};

- 1
- 2
- 3
- 4 **(correct)**
