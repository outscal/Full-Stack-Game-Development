# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. What is function overloading in C++?
- Defining multiple functions with the same name but different return types.
- Defining multiple functions with the same name but different parameter types or numbers. **(correct)**
- Defining multiple functions with different names and the same parameter types.
- Defining multiple functions with the same name and identical parameters but in different classes.

### Q2. Can function overloading be done based solely on the return type in C++?
- Yes, overloading based solely on return type is allowed.
- No, the return type alone cannot be used to distinguish overloaded functions. **(correct)**
- Yes, but only for template functions.
- It depends on the compiler.

### Q3. What will be the output of the following code?

#include <bits/stdc++.h>
using namespace std;

void print(int a) {
    cout << "int: " << a << endl;
}

void print(double a) {
    cout << "double: " << a << endl;
}

int main() {
    print(5.0);
    return 0;
}
- double: 5
- int: 5
- double: 5.0 **(correct)**
- int: 5.0
