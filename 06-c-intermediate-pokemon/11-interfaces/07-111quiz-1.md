# [⏳Quiz]

[⏳Quiz]


<div class="outscal-quiz" data-total="4">

<div class="oq-q" data-idx="0" data-answers="2">
<h3>Q1. Which of the following statements is false about interfaces in C++?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">A class implementing an interface must define all its pure virtual functions.</button>
  <button type="button" class="oq-opt" data-i="1"> A class can implement multiple interfaces through multiple inheritance.</button>
  <button type="button" class="oq-opt" data-i="2">An interface can have member variables.</button>
  <button type="button" class="oq-opt" data-i="3">An interface cannot be instantiated directly but can point to objects of implementing classes.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="1" data-answers="3" hidden>
<h3>Q2. In C++, which statement is true about interfaces and inheritance?</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">A class that implements an interface cannot inherit from another class.</button>
  <button type="button" class="oq-opt" data-i="1"> A class can inherit multiple interfaces, but they must be from the same hierarchy.</button>
  <button type="button" class="oq-opt" data-i="2"> A class can inherit multiple interfaces without implementing all their pure virtual functions and still be instantiated.</button>
  <button type="button" class="oq-opt" data-i="3"> A class can inherit multiple interfaces but must implement all pure virtual functions to be instantiated.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="2" data-answers="2" hidden>
<h3>Q3. What will be the output of the code?

#include &lt;iostream&gt;

class Base {
public:
    virtual void show() {
        std::cout &lt;&lt; &quot;Base class&quot; &lt;&lt; std::endl;
    }
    virtual ~Base() {
        std::cout &lt;&lt; &quot;Base destructor&quot; &lt;&lt; std::endl;
    }
};

class Derived : public Base {
public:
    void show() override {
        std::cout &lt;&lt; &quot;Derived class&quot; &lt;&lt; std::endl;
    }
    ~Derived() {
        std::cout &lt;&lt; &quot;Derived destructor&quot; &lt;&lt; std::endl;
    }
};

int main() {
    Base* obj = new Derived();
    obj-&gt;show();
    delete obj;
    return 0;
}
</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Derived class, Base destructor</button>
  <button type="button" class="oq-opt" data-i="1">Base class   Derived destructor   Base destructor</button>
  <button type="button" class="oq-opt" data-i="2">Derived class  Derived destructor  Base destructor</button>
  <button type="button" class="oq-opt" data-i="3">Undefined behavior because Derived is not properly destroyed.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-q" data-idx="3" data-answers="0" hidden>
<h3>Q4. What will be the output of the above code?

#include &lt;iostream&gt;

class IShape {
public:
    virtual double area() const = 0;  // Pure virtual function
    virtual void display() const = 0; // Pure virtual function
    virtual ~IShape() = default;
};

class Circle : public IShape {
private:
    double radius;
public:
    Circle(double r) : radius(r) {}
    double area() const override { return 3.14 * radius * radius; }
    void display() const override { std::cout &lt;&lt; &quot;Circle&quot; &lt;&lt; std::endl; }
};

class Square : public IShape {
private:
    double side;
public:
    Square(double s) : side(s) {}
    double area() const override { return side * side; }
    void display() const override { std::cout &lt;&lt; &quot;Square&quot; &lt;&lt; std::endl; }
};

void printShapeArea(const IShape&amp; shape) {
    shape.display();
    std::cout &lt;&lt; &quot;Area: &quot; &lt;&lt; shape.area() &lt;&lt; std::endl;
}

int main() {
    Circle c(5.0);
    Square s(4.0);

    IShape* shape1 = &amp;c;
    IShape* shape2 = &amp;s;

    printShapeArea(*shape1);
    printShapeArea(*shape2);

    return 0;
}
</h3>
<div class="oq-opts">
  <button type="button" class="oq-opt" data-i="0">Circle   Area:  78.5   Square   Area: 16</button>
  <button type="button" class="oq-opt" data-i="1">Area: 78.5    Area: 16</button>
  <button type="button" class="oq-opt" data-i="2">Compilation error because IShape has pure virtual functions.</button>
  <button type="button" class="oq-opt" data-i="3">Undefined behavior due to missing destructor.</button>
</div>
<button type="button" class="oq-submit" disabled>Submit</button>
<div class="oq-feedback" hidden></div>
<button type="button" class="oq-next" hidden>Next →</button>
</div>

<div class="oq-done" hidden>
<h2>Quiz Complete</h2>
<p>You scored <span class="oq-score">0</span> out of <span class="oq-total">4</span>.</p>
</div>
</div>
