# Sorting Quiz

Sorting Quiz


## Questions


### Q1. What would be the time complexity to build a max or min-heap from an array of N integers?
- O(N)  **(correct)**
- O(N (log N)) 
- O(N^2) 
- O(log N)

### Q2. Given all the elements of an array (of size N) lie between 1 to 500. Which sorting algorithm should be used to sort the array (or which sorting algorithm will be preferred)?
-  Merge Sort 
- Counting Sort  **(correct)**
- Quick Sort 
- Insertion Sort

### Q3. In a binary max-heap containing N numbers, the smallest element can be found in time?
- O(1) 
- O(log N) 
- O(N (log N)) 
- O(N) **(correct)**

### Q4. Which one of the following arrays represents a binary max-heap?
- [26, 13, 17, 14, 11, 9, 15] 
- [26, 15, 17, 14, 11, 9, 13]  **(correct)**
- [26, 15, 14, 17, 11, 9, 13] 
- [26, 15, 13, 14, 11, 9, 17]

### Q5. What is the best time complexity possible for Bubble Sort?
-  O(N^2) 
- O(N (log N)) 
- O(N)  **(correct)**
- None of the above

### Q6. Which one of the following in place sorting algorithms needs the minimum number of swaps?
- Insertion Sort 
- Selection Sort   **(correct)**
- Merge Sort
- Heap Sort

### Q7. A sorting technique is called stable if?
- It uses the divide and conquer technique 
- it uses O(n) space
- it takes O(n log n) time to sort 
- Relative order of occurrence of non-distinct elements is maintained **(correct)**

### Q8. What is the worst-case time complexity of Quick Sort?
-  O( N^2 )  **(correct)**
- O(N log N ) 
- O( N (log N)^2 ) 
- O( N )
