# ⏳Quiz

⏳Quiz


## Questions


### Q1. 1. Imagine you are developing different software applications. Match each application with the type of data structure (Primitive, Non-Primitive Linear, or Non-Primitive Non-Linear) it most likely utilizes for its core operations:
    
    1) A calculator app that needs to store and manipulate simple numerical values for operations like addition and subtraction.
    
    2) An email client where emails are listed in the order they are received, and you can scroll through them one by one.
    
    3) A navigation app that calculates the fastest route from point A to point B, considering various possible paths and obstacles.
    
    4) A video game that needs to keep track of the score of a player in a session.


Choose the correct option below:
- 1-Primitive, 2-Non-Primitive Linear, 3-Non-Primitive Non-Linear, 4-Primitive **(correct)**
- 1-Non-Primitive Non-Linear, 2-Primitive, 3-Non-Primitive Linear, 4-Non-Primitive Non-Linear
- 1-Non-Primitive Linear, 2-Primitive, 3-Non-Primitive Non-Linear, 4-Non-Primitive Linear
- 1-Non-Primitive Linear, 2-Non-Primitive Non-Linear, 3-Primitive, 4-Non-Primitive Linear

### Q2. Which statement best aligns with the foundational concept of a data structure?
- A low-level hardware component primarily responsible for converting binary data into human-readable formats.
- A protocol used by operating systems to load and unload processes from memory.
- A specialized algorithm that automatically encrypts and decrypts any given data.
- A conceptual and organized way to represent data that helps manage and access it efficiently in computing tasks. **(correct)**

### Q3. From a broad theoretical perspective, data structures are often divided into categories. Which option best captures these major categories?
- Data structures are grouped based on whether they are used on desktop operating systems or mobile operating systems.
- Data structures are classified by how quickly they can be serialized for network transmission.
- Data structures are typically split into “linear” (e.g., arrays, queues) and “non-linear” (e.g., trees, graphs) based on how elements are conceptually connected. **(correct)**
- Data structures are categorized purely by whether they can store numeric or textual data.

### Q4. How do static data structures fundamentally differ from dynamic data structures in theoretical terms?
- Static data structures have a size fixed at creation, while dynamic data structures can adjust their size at runtime, allowing flexibility. **(correct)**
- Static data structures are only suitable for functional programming languages, whereas dynamic data structures are exclusive to object-oriented languages.
- Static data structures always store data in contiguous physical locations, while dynamic data structures use distributed cloud storage.
- Static data structures are read-only, while dynamic data structures are write-only.

### Q5. When discussing linear vs. non-linear data structures from a purely theoretical standpoint, which statement is most accurate?
- Linear data structures can store data across multiple disconnected memory segments, while non-linear structures must be contiguous.
- Linear data structures arrange elements in a sequence (one after another), whereas non-linear data structures involve hierarchical or relational links (like trees or graphs). **(correct)**
- Non-linear data structures can only handle a single data type, while linear data structures support multiple data types simultaneously.
- Linear data structures execute their operations exclusively in constant time, whereas non-linear data structures require polynomial time.

### Q6. Why do we distinguish between Abstract Data Types (ADTs) and their implementations?
- ADTs handle all low-level memory management, while implementations only deal with high-level code syntax.
- ADTs are taught only in theoretical computer science courses, whereas implementations are used in real-world software.
- There is no significant difference; “ADT” and “implementation” are interchangeable terms.
- ADTs specify what operations are available and how they conceptually behave, while implementations detail how these operations are actually performed in a specific language or environment. **(correct)**
