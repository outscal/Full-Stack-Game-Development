# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. What is the time, space complexity of the following code:

int a = 0, b = 0;
for (i = 0; i < N; i++) {
    a = a + rand();
}
for (j = 0; j < M; j++) {
    b = b + rand();
}
- O(N * M) time, O(1) space
- O(N + M) time, O(N + M) space
- O(N + M) time, O(1) space **(correct)**
- O(N * M) time, O(N + M) space

### Q2. What is the time complexity of deleting and element form an array and why?
- O(n) - Because deleting an element requires shifting all subsequent elements in the array to fill the gap left by the deleted element. **(correct)**
- O(1) - Since arrays provide constant-time access to elements, deletion can also be accomplished in constant time.
- O(log n) - As modern data structures employ efficient deletion algorithms such as binary search trees to optimize the process.
- O(n^2) - Due to the nested iterations required to relocate elements after deletion, leading to a quadratic time complexity.

### Q3. How is time complexity measured?
- By counting the number of algorithms in an algorithm.
- By counting the number of primitive operations performed by the algorithm on given input size. **(correct)**
- By counting the size of data input to the algorithm.
- none of the above

### Q4. Which of the following operations are O(1) for arrays?
- Searching any element
- Accessing any element **(correct)**
- Inserting an element
- deleting an element
