# ⏳Quiz

⏳Quiz


## Questions


### Q1. In a typical Minesweeper implementation, what happens if the user tries to open a cell that is currently flagged?
- The game automatically removes the flag and opens the cell without warning.
- The game does not allow opening a flagged cell; you must remove the flag first. **(correct)**
- The flagged cell is opened and immediately converted to a hidden state again.
- The game randomly decides whether to open the cell or keep it flagged.

### Q2. In SFML, if you pass a negative width (e.g., IntRect(50, 0, -32, 32)) to sf::Sprite::setTextureRect(), what is the intended effect?
- SFML flips the sprite horizontally, rendering it mirrored. **(correct)**
- SFML clamps the width to zero, effectively drawing nothing.
- SFML immediately throws a runtime exception and stops the program.
- SFML interprets negative values as an error and switches to the default texture region.

### Q3. What happens in SFML if the rectangle passed to setTextureRect() extends beyond the original texture boundary (e.g., width or height goes past the edge)?
- SFML simply renders the valid portion of the texture and clips anything out of bounds. **(correct)**
- SFML tiles the texture automatically to fill the extra space.
- SFML expands the texture at runtime to accommodate the rectangle.
- SFML renders an error image or a red placeholder for missing pixels.

### Q4. Which statement best describes the difference between an sf::FloatRect and an sf::IntRect in SFML?
- An sf::FloatRect cannot be used for collision detection, while an sf::IntRect can.
- An sf::IntRect automatically snaps objects to the closest pixel, while sf::FloatRect only stores the coordinates and size.
- An sf::FloatRect uses floating-point values for coordinates and size, while an sf::IntRect uses integer values. **(correct)**
- They are identical; one is just an alias of the other in different parts of the code.
