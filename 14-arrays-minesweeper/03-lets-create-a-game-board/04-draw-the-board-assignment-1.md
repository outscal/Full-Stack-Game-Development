# Initial Instructions


---

- Make sure you are in the  `Feature_1-Board`  branch in your git repo.
- Make changes mentioned in the assignment (Add new commits) on this branch.
- Create a `Pull Request`, from this feature branch `Feature_1-Board` to the previous branch `main` if not already created.
- If you had created a PR previously, no need to create again, simply commit your changes and that PR will be updated.
- Since this is a guided assignment, code reviews will not happen, instead, the solution code is present in the assignment.
  [**Click Here To See The Solution Code**](https://github.com/outscal/Arrays/tree/Feature-1_Board/Array-Minesweeper)
- But, before checking the solution code, try your best to solve it independently.



> 💡**PROTIP:**
> You can spoil yourself by looking at the [**solution**](https://github.com/outscal/Arrays/tree/Feature-1_Board/Array-Minesweeper) before trying out the assignments, but we don't recommend learning that way.

> If you feel stuck somewhere, don't worry 😌

> It's a part of the learning process!

> Clear your doubts [**here**](https://discord.com/channels/536834108077113364/680360808936767496). It's a very helpful and inviting place!





## Draw the Board


---

## [tasks]



`Board.cpp`

1. Implement `initializeBoardImage()` method. 
2. Implement `initialize()` method. 
3. In `Board()` constructor call `initialize()` method. 
4. In `Board::render()` draw the `boardSprite`



`GameplayManager.cpp`

1. Implement the `initialize()` and `initializeVariables()`function. 
2. In `GameplayManager::render()` call the `Board` class’s `render()` function.



## Draw the Background Image


---

## [tasks]



`Board.cpp`

1. Implement `initializeBackgroundImage()` method. 
2. In `GameplayManager::render()` draw the `background_sprite`



# Submission Instructions


---

> Submit the Pull Request (PR) link after pushing all the changes in the specified branch.
