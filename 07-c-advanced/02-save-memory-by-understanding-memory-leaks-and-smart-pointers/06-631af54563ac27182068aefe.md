# [Quiz] Memory Leaks

Memory Leaks


## Questions


### Q1. What is the output of the below code?

#include <iostream>
class Outscal {
public:
    Outscal () {
        std::cout << "1 ";
    }
};
class OutscalGame : public Outscal {
public:
    OutscalGame() {
        std::cout << "2 ";
    }
};
class OutscalDev : public OutscalGame {
public:
    OutscalDev() {
        std::cout << "3\\n";
    }
};
void test () {
    OutscalGame clan;
    std::cout << '\\n';
}
int main() {
    OutscalDev clan1;
    test();
    return 0;
}

a) 1 2
    1 2 3
b) 3
    2
c) 2
    3
d) 1 2 3
    1 2

- a
- b
- c
- d **(correct)**

### Q2. What is the output of the below code?

#include <iostream>
class Outscal {
public:
    ~Outscal () {
        std::cout << "1 ";
    }
};
class OutscalGame : public Outscal {
public:
    ~OutscalGame() {
        std::cout << "2 ";
    }
};
class OutscalDev : public OutscalGame {
public:
    ~OutscalDev() {
        std::cout << "3 ";
    }
};
void test () {
    OutscalGame clan;
}
int main() {
    OutscalDev clan1;
    test();
    return 0;
}
- 3 2
- 2 3
- 1 2 1 2 3
- 2 1 3 2 1 **(correct)**

### Q3. In which memory dynamic allocation happens?
- Heap **(correct)**
- Permanent storage area
- Free memory
- Stack

### Q4. In which memory Local variables are stored?
- Heap
- Permanent storage area
- Free memory
- Stack **(correct)**

### Q5. What is the correct syntax for using smart pointers in C++?
        (Assume header files are already included)
- std::unique_ptr<int> p(new int);  **(correct)**
- std::unique_ptr<int> p[new int];
- std::unique_ptr<int> p = new int;
- std::unique_ptr<int> p = new int[];

### Q6. Which statements should be added to avoid memory leaks in the below code?

#include <iostream>
class Outscal {
};
int main() {
    Outscal *ptr1 = new Outscal;
    Outscal game1;
    return 0;
}

- delete game1;
- delete ptr1; **(correct)**
- delete game1;     delete ptr1;
- There is no memory leak.

### Q7. When does memory leak occur?
-  Memory is allocated and then goes out of scope before it is released. **(correct)**
- Memory is allocated and then is released before it goes out of scope.
-  A variable is defined locally, and then is changed in a global context.
- A variable is defined globally, and then is changed in a local context.

### Q8. What is the output of the below code?

#include <iostream>
#include <memory>
class Outscal {
    public:
        Outscal () {
            std::cout << "Creating object \n";
        }
        ~Outscal () {
            std::cout << "Deleting object \n";
        }
};
void test() {
    std::unique_ptr<Outscal> object1(new Outscal());
}
int main() {
    test();
    Outscal obj;
    return 0;
}

a)  Creating object
     Creating object
     Deleting object
     Deleting object

b)  Deleting object
     Creating object
     Creating object
     Deleting object

c)  Creating object
     Deleting object
     Creating object
     Deleting object

d)  Creating object
     Creating object
     Deleting object

- a
- b
- c **(correct)**
- d
