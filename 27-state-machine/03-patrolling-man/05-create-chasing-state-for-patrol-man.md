> In your ***Patrol-Man-Setup**** *branch complete the following tasks, commit all these changes and submit the link of your branch for this assignment.



**Tasks:**

- Update `OnePunchManStates` to `States` & add Patrolling and Chasing states in that enum.
- Use the base class `EnemyController` to represent an enemy in `IState` instead of `OnePunchManController`.
- Create an interface `IStateMachine` and make all state machines implement this interface.
- Refactor all your state classes and state machines by updating references of `IStateMachine` and `EnemyController`.



- **Create the following new states:**
- Patrolling State
- Chasing State



![](https://tenor.com/view/working-from-home-computer-homework-gif-13271998.gif)
