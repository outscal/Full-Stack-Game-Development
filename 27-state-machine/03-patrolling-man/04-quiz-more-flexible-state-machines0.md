# Quiz - more flexible state machines

Quiz - more flexible state machines


## Questions


### Q1. In the updated PatrollingState class, when does the state transition to the Idle State?
- When the enemy reaches its destination. **(correct)**
- When the enemy completes a full patrol loop.
- When the MoveTowardsDestination() method is called.
- When the OnStateEnter() method is invoked.

### Q2. Which programming principle are we using to make our State Machine architecture more flexible?
- Don't repeat yourself principle
- Inheritance over Composition
- Program to an interface, not an implementation **(correct)**
- Single Responsibility Principle
