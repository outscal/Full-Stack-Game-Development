# [Quiz] Player Movement

Player Movement


## Questions


### Q1. Observe the below and chose the correct answer.

private void Move(){
	Vector3 position = transform.position;
	position.x += horizontal * speed * Time.deltaTime;
	transform.position = position;
}

- Moves the character along the x-axis **(correct)**
-  Moves the character along the y-axis
-  Moves the character along the z-axis
-  All of the above

### Q2. Time.deltaTime can be defined as
- The interval in seconds from the last frame to the current frame **(correct)**
- The interval in seconds from the current frame to the next frame
-  The interval in seconds between any two frames
- The interval in seconds between the first frame and last frame.

### Q3. Time.deltaTime makes gameobject frame dependent?
-  True
-  False **(correct)**

### Q4. Which would be the right approach from the below given code?

a)  
private Rigidbody2D rigidbody;
private void Awake(){
	rigidbody = gameobject.GetComponent<Rigidbody2D>();
}
private void Update(){
	rigidbody.Addforce(new Vector2(0f, 10), ForceMode2D.Impulse);
}

b)
private Rigidbody2D rigidbody;
private void Update(){
	rigidbody = gameobject.GetComponent<Rigidbody2D>();
}
private void Awake(){
	rigidbody.Addforce(new Vector2(0f, 10), ForceMode2D.Impulse);
}

c)
private Rigidbody2D rigidbody;
private void FixedUpdate(){
	rigidbody = gameobject.GetComponent<Rigidbody2D>();
}
private void Update(){
	rigidbody.Addforce(new Vector2(0f, 10), ForceMode2D.Impulse);
}

d)
private Rigidbody2D rigidbody;
private void Awake(){
	rigidbody = gameobject.GetComponent<Rigidbody2D>();
}
private void Start(){
	rigidbody.Addforce(new Vector2(0f, 10), ForceMode2D.Impulse);
}
- a) **(correct)**
- b)
- c)
- d)

### Q5.  Which sub-component would you use when you want to restrict the character from rotating on a certain axis?
-  Interpolates
-  Colliders
- Constraints   **(correct)**
- None

### Q6. What is the use of Mathf.Abs Function?
-  Turns positive value into Negative
-  Turns Negative value into positive **(correct)**
- Both
- None

### Q7. Below code makes your player character jump but there is an error, find out the correct implementation:

public int jump;

if(vertical > 0)
{
	rigidbody.Addforce(new Vector2(jump,0), ForceMode2D.Force)
}
-  (new Vector2(0, jump) **(correct)**
- (new Vector2(jump, 0)
- (new Vector3(jump, 0, 0)
-  None

### Q8.  What component would change if you want the player character to land quickly after the jump?
-  Decrease the mass
-  Freeze Rotation
-  Increase the mass **(correct)**
- None
