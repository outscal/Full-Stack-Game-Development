# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. Which scenario is most likely to demonstrate the inefficiency of using the FindObject() method in a large Unity scene?
- Searching for an object that is at the beginning of the hierarchy
- Searching for an object that does not exist in the scene
- Searching for an object that is at the end of the hierarchy
- Both b) and c) **(correct)**

### Q2. How does the linear search algorithm behave if the list contains multiple instances of the target element?
- It returns the index of the first occurrence **(correct)**
- It returns the index of the last occurrence
- It returns all indices where the element is found
- It throws an error

### Q3. The StickCollectionController class has member variables for search_type and stick_to_search. How are these used together to implement a search algorithm?
- search_type identifies the stick to search for, and stick_to_search holds the result.
-  search_type determines the search algorithm, and stick_to_search stores the target value to compare sticks with. **(correct)**
- stick_to_search is not required for search algorithm here.
- Both variables are used to store temporary data during the search process.

### Q4. How would you modify the linear search visualization project to highlight both the current stick being checked and the previous stick checked?
- Add a delay between checking each stick
- Keep the previous stick highlighted until the search ends
- Use a flashing animation for the current stick
- Implement a secondary color for the previous stick **(correct)**

### Q5. Which factors influence the width of each stick calculated by the "calculateStickWidth" method?
- Number of sticks and screen space.
- Screen space and spacing between sticks.
- Both a & b **(correct)**
- The method doesn't consider any of these factors.

### Q6. Suppose you’re applying linear search on linked list. The pointer of the last node points back to the first node, creating a cycle. What will be the result of linear search in such condition when the value you’re searching for is not present in the list?
- The search will successfully terminate, returning -1.
- The program will crash.
- The search gets stuck in an infinite loop, continuously iterating through the cycle. **(correct)**
- A random value will be returned.

### Q7. What is the primary reason for initializing the random seed before shuffling the sticks?
- To ensure the sticks are shuffled in a different order each time the program runs **(correct)**
- To sort the sticks in increasing order before shuffling
- To maintain a fixed shuffle order across multiple runs
- To synchronize the shuffling process with other game elements

### Q8. What is the primary advantage of using multithreading in game development?
- To reduce the complexity of the code
- To increase the memory usage
- To improve performance by parallelizing tasks **(correct)**
- To ensure tasks are executed sequentially

### Q9. In the context of visualizing a search algorithm with multithreading, what could happen if the search thread does not join the main thread correctly?
- The main thread will wait indefinitely, freezing the program
-  The search results might be displayed multiple times
- The visualization might complete without showing the progress **(correct)**
- The search thread will run in parallel with the main thread without any issues

### Q10. During a multithreaded linear search, what is the purpose of introducing a delay in the search thread?
- To ensure the search results are accurate
- To provide visual feedback by pausing between each comparison **(correct)**
- To allow the main thread to catch up with the search thread
- To slow down the algorithm and reduce CPU usage
