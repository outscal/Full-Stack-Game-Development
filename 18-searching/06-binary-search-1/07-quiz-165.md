# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1.  What is the primary advantage of binary search compared to linear search for finding an element in a sorted list?
- Binary search requires less memory space.
- Binary search significantly reduces the number of comparisons needed in the average case. **(correct)**
- Binary search guarantees finding the target element.
- Binary search works on both sorted as well as unsorted array.

### Q2. What condition must be met for Binary Search to work correctly?
- The list must be sorted in ascending or descending order **(correct)**
- The list must contain unique elements
- The list must be small in size
- The list must be unsorted

### Q3. Suppose you're implementing Binary Search in a game. Players search for a target enemy among a sorted list of enemies based on their strength. What is the role of the compareElementsByData() function in this context?
- It sorts the enemies based on their strength.
- It determines the strength of each enemy.
- It compares the strengths between two enemies. **(correct)**
- It splits sorted listed of enemies into low and high based on strength.  

### Q4. The time complexity of binary search is expressed as O(log n). What does this logarithmic growth indicate?
- The number of comparisons increases exponentially with the array size. 
- The number of comparisons increases slowly relative to the increase in array size. **(correct)**
- The number of comparisons remains constant.
- None of these.

### Q5. If the binary search algorithm is applied to an unsorted list, what would be the result?
- The search will still find the target element efficiently
- The search will fail to find the target element correctly **(correct)**
- The search will sort the list before finding the target
- The search will take longer but eventually find the target

### Q6. What is the worst-case number of comparisons made by a binary search algorithm in an array of size 1,000,000 elements?
- 19
- 20
- 21 **(correct)**
- 22

### Q7. What would happen if you try to use Binary Search on a linked list instead of an array?
- The search will be more efficient due to the structure of the linked list
- The search will work the same as with an array
- The search will automatically sort the linked list first
- The search will be less efficient because accessing elements is not constant time **(correct)**
