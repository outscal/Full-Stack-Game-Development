**ASSIGNMENT LINK:** [https://outscal.com/compiler/c-sharp-beginner](https://outscal.com/compiler/c-sharp-beginner) 



# **Tasks**


---

## Setup Game Loop


---

## [In Game Class]

1. Create an empty `public void GameLoop()` function.



## [In Program Class]

1. In `Main()` function:
2. 1. Clear everything except for the object of `Game` class.
  2. Call the `game.GameLoop()` function.





## Display Game Story


---

## [In Game Class]

1. In `GameLoop()` function call `DisplayGameStory()` function.





## Implement Start Menu


---

## [In Game Class]

1. Create `bool isGameExited`
2. Implement the `ExitGame()` function.
3. Implement the `private void StartMenu()` function.
4. Implement the `private void ProcessStartMenuInput()` function.
5. In `StartMenu()` print the start menu and call the `ProcessStartMenuInput()` after printing the menu.
6. In the `GameLoop()` function:
7. 1. Create a `while(!isGameExited)` loop
  2. Put the `DisplayGameStory()` and `StartMenu()` in the `while` loop.



## Implement reStart Menu


---

## [In Game Class]

1. Implement the `private void RestartMenu()`  function. 
2. Implement the `private void ProcessRestartMenuInput()` function.
3. In `GameLoop()` function call the `RestartMenu()` function after `StartMenu()` if the game is not exited.



Expected Output.


<iframe src="https://www.loom.com/embed/760b75558d0543bf8c1c1e31de6fca4f" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>



**Game Loop**





# Submission Instructions


---

- Save the code in the above compiler link
- Submit the Submission Link from the compiler
