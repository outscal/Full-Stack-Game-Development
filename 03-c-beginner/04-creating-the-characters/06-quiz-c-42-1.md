# [⏳Quiz]

   


## Questions


### Q1. Which of the following is TRUE about the destructor (finalizer) in C#?
- It must be called explicitly in your code
- It is called by the garbage collector automatically when an object is collected **(correct)**
- C# does not support destructors
- Destructors in C# work exactly the same way as in C++

### Q2. What is the role of a property in a C# class?
- To provide logic to handle both getting and setting private fields **(correct)**
- To declare public variables directly
- To create multiple constructors for a class
- To handle unmanaged resources like files or network connections

### Q3. What happens when you use the new keyword in C#?
- It declares a new variable without allocating memory
- It initializes a new object, calls the constructor, and allocates memory for it **(correct)**
- It only allocates memory but does not call the constructor
- It copies an existing object

### Q4. What is the difference between declaring and initializing an object in C#?
- Declaration allocates memory, but initialization does not
- Declaration uses the new keyword, while initialization does not
- Initialization uses the new keyword and allocates memory, while declaration only creates a reference without allocating memory **(correct)**
- There is no difference between declaration and initialization
