# [⏳Quiz]

 


## Questions


### Q1. Which of the following is the correct flow of Battle Loop?
- Show Battle Options → Show Stats → Enemy Attacks → Take Input for Player’s Choice → Player Attacks or Heals → Check Game Over
- Show Battle Options → Take Input for Player’s choice → Player Attacks or Heals → Enemy Attacks → Show Stats → Check Game Over **(correct)**
-  Show Stats→ Show Battle Options → Enemy Attacks → Take Input for Player’s Choice → Player Attacks or Heals → Check Game Over
- Show Stats→ Show Battle Options → Enemy Attacks → Check Game Over → Take Input for Player’s Choice → Player Attacks or Heals

### Q2. What is the primary purpose of the Console.ReadKey() method in C#?
- To read an entire line of text input from the user.
- To read a single character from the user input and return the character.
- To pause the execution of the program until the user presses any key, and then return the character of the key that was pressed. **(correct)**
- To read multiple keys pressed simultaneously by the user.

### Q3. What happens when the player selects 'A' during their turn?
- Dough Master heals themselves
- Dough Master attacks the Crust Bandit **(correct)**
- Crust Bandit takes two turns
- The game restarts

### Q4. In a game's loot drop system, the following switch statement is used to determine the type of loot dropped:

int lootCode = 4;
string lootType;

switch (lootCode)
{
    case 1:
    case 2:
        lootType = "Common Loot";
        break;
    case 3:
        lootType = "Rare Loot";
        break;
    case 4:
    case 5:
    case 6:
        lootType = "Epic Loot";
        break;
    default:
        lootType = "Unknown Loot";
        break;
}

Console.WriteLine($"Loot Dropped: {lootType}");

Which of the following lootCode values will result in "Epic Loot" being printed? (Select all that apply)
- 4
- 2
- 6
- Both 1 and 3 **(correct)**
