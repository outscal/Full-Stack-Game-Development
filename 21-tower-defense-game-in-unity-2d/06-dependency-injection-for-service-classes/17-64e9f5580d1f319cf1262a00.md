# [Quiz] UIService Hint

UIService Hint


## Questions


### Q1. Why are we not injecting dependencies of WaveService in its constructor, instead of using the Init() method?
- Using method dependency injection is always preferred over constructor dependency injection.
- We should be using constructor dependency.
- There will be cross dependency which will cause null reference error in the Game Service class. **(correct)**
- It won’t make any difference either way.

### Q2. When should the SubscribToEvents() method be called in UIService?
- It should be called in the Awake() method before anything else in this class is executed.
- Before initializing its dependencies.
- Once all the dependencies have been initialized. **(correct)**
- The order of execution does not matter in this case.
