# [Quiz] Implementing Generic Singleton

Implementing Generic Singleton


## Questions


### Q1. In the script, what is the significance of the constraint where T : GenericMonoSingleton<T>?
- It restricts the use of the script to only non-generic MonoBehaviour classes.
- It enforces that the script can only be used with classes derived from GenericMonoSingleton<T> **(correct)**
- It limits the script's usage to specific namespaces within Unity.
- It ensures that the script can't be used with MonoBehaviour classes at all.

### Q2. What is the purpose of the Instance property in the GenericMonoSingleton<T> class?
- It refers to the main instance of the Unity game.
- It provides access to the current instance of the MonoBehaviour class. **(correct)**
- It handles the rendering order of objects in the scene.
- It synchronizes the execution of the Awake() method in MonoBehaviour classes.
