# [Quiz] Function of Strings

Function of Strings


## Questions


### Q1. What will be the output of the following C++ code?

#include <iostream>
#include <string>
using namespace std;
int main() {
    string str {"Steve jobs"};
    unsigned long int found = str.find_first_of("aeiou");
    while (found != string :: npos)
    {
        str[found] = '*';
        found = str.find_first_of("aeiou", found + 1);
    }
    cout << str << "\n";

    return 0;
}
- Steve
- jobs
- St*v* j*bs **(correct)**
- St*v*

### Q2. What is the output of the below code?

#include <bits/stdc++.h>
using namespace std;
int main () {
	char str1[10] = "Hello";
	char str2[10] = "World";
	char str3[10];
	int len ;
	strcpy( str3, str1);
	strcat( str1, str2);
	len = strlen(str1);
	cout << len << endl;
	return 0;
}
- 5
- 55
- 11
- 10 **(correct)**

### Q3. What is the output of the below code?

#include <bits/stdc++.h>
using namespace std;
int main () {
	string str ("microsoft");
	string::reverse_iterator r;
	for (r = str.rbegin() ; r < str.rend(); r++ )
		cout << *r;
	return 0;
}
- microsoft
- micro
- tfosorcim **(correct)**
- tfos

### Q4. What is the output of the below code?

#include <bits/stdc++.h>
using namespace std;
int main () {
	string str ("Microsoft");
	for (size_t i = 0; i < str.length();) {
		cout << str.at(i-1);
	}
return 0;
- M
- Microsoft
- Micro
- runtime error **(correct)**

### Q5. Which method do we use to append more than one character at a time?
- append
- operator+=
- data
- both a & b **(correct)**

### Q6. Strcat() function adds null character.
- Only if there is space
- Always **(correct)**
- Depends on the standard
- Depends on the compiler

### Q7. What is the output of following program?

# include <stdio.h>
int main() {
   char str1[] = "ArrayQuiz";
   char str2[] = {'A', 'r', 'r', 'a', 'y', 'Q', 'u', 'i', 'z'};
   int n1 = sizeof(str1)/sizeof(str1[0]);
   int n2 = sizeof(str2)/sizeof(str2[0]);
   printf("n1 = %d, n2 = %d", n1, n2);
   return 0;
}
- n1 = 10, n2 = 9 **(correct)**
- n1 = 10, n2 = 10
- n1 = 9, n2 = 9
- n1 = 9, n2 = 10

### Q8. Predict the output:

#include <stdio.h>
int fun(char *str1) {
  char *str2 = str1;
  while(*++str1);
  return (str1-str2);
}
 
int main() {
  char *str = "ArrayQuiz";
  printf("%d", fun(str));
  return 0;
}
- 10
- 9  **(correct)**
- 8
- Random Number
