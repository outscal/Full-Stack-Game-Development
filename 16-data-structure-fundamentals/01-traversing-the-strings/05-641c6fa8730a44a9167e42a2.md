# [Quiz] Strings

Strings


## Questions


### Q1. What is the output of the below code?

#include <bits/stdc++.h>
int main() {
	char str[] = {'a', 'b', 'c'};
  std::cout << str << '\n';
	return 0;
}
- abc
- abcgarbage **(correct)**
- garbage
- none of the above

### Q2. What is the time complexity of concatenation of string in C++?

// E.g find time complexity of below code.
string str = "outscal";
str += "clan";
- O(1)
- O(M) → M is the length of string to be concatenated
- O(N) → N is the length of the original string
- Unspecified, but generally linear **(correct)**

### Q3. What should be filled in place of _______ to run the below code without any error?

#include <bits/stdc++.h>
using namespace std;
int main() {
	string s1 = "oustcalclan";
  string s2 = "clann";
  size_t pos = s1.find(s2);
  if (pos == _______)
      cout << "Not Found" << '\n';
  else 
      cout << "Found" << '\n';
	return 0;
}
- -1
- string::size_t
- s1.length()
- string::npos **(correct)**

### Q4. What is the output of the below code?

// stoi is used to convert string into integer
#include <bits/stdc++.h>
using namespace std;
int main() {
	string s = "0x10A";
  cout << stoi(s, nullptr, 16) << endl;
	return 0;
}
- Compilation error
- Garbage Value
- 266 **(correct)**
- 1010

### Q5. Predict the output:

#include <bits/stdc++.h>
using namespace std;
int main() {
	char str[] = "abc";
  cout << sizeof(str) << " " << strlen(str) << '\n';
	return 0;
}
- 3 3
- 4 3 **(correct)**
- 3 4
- 4 4

### Q6. The capacity of a string object is equal to the _______ .
- String length + 1
- String length **(correct)**
- String length - 1
- 1000

### Q7. Predict the output:

#include <bits/stdc++.h>
using namespace std;
int main() {
	char str[] = "GAME DEVELOPER";
  char* token = strtok(str, " ");
  while (token != NULL) {
      cout << token << '\n';
      token = strtok(NULL, " ");
  }
	return 0;
}

a)GAME DEVELOPER

b)GAMEDEVELOPER

c)GAME

d)GAME
DEVELOPER
- a)
- b)
- c)
- d) **(correct)**

### Q8. What is the output of the below code?
#include <bits/stdc++.h>
int main() {
	char str[] = "abc";
  std::cout << str << '\n';
	return 0;
}
- abc **(correct)**
- abcgarbage 
- garbage 
- none of the above
