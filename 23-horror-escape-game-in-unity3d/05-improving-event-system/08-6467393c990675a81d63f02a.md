# [Quiz] Making Event Service

Making Event Service


## Questions


### Q1. How can an external class subscribe to the LightSwitchToggleEvent using the EventService.Instance?
- EventService.Instance.LightSwitchToggleEvent.AddListener(someMethod); **(correct)**
- EventService.Instance.AddListener(LightSwitchToggleEvent, someMethod);
- EventService.Instance.LightSwitchToggleEvent += someMethod;
- EventService.Instance.SubscribeToEvent(LightSwitchToggleEvent, someMethod);

### Q2. What is the purpose of the EventService.Instance property?
- It provides access to the game event instances. **(correct)**
- It handles the event subscription logic.
- It triggers all the game events simultaneously.
- It initializes the game event controller instances.
