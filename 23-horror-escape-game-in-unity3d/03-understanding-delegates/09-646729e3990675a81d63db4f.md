# [Quiz] Adding a listener to PlayerController

Adding a listener to PlayerController


## Questions


### Q1. In the Light Switch example for Observer pattern, which component acts as the subject?

> Hint: [Subject = Object being Observed]

- Player Controller
- Light Source
- Light Switch **(correct)**
- Switch Board

### Q2. In C#, what is multicast delegate?
- A delegate that can be used to invoke methods in different languages.
- A delegate that can be used to invoke multiple methods. **(correct)**
- A delegate that can be used to invoke methods asynchronously.
- A delegate that can be used to invoke methods from different namespaces.
