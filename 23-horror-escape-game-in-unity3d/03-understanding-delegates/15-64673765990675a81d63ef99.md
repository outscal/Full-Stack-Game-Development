# [Quiz] Good Practices while using Delegate / Events

Good Practices while using Delegate / Events


## Questions


### Q1. What is the key difference between the Action delegate and basic delegates in C#?
- Action is a generic delegate, while basic delegates are non-generic.
- Action is a single-shot delegate, while basic delegates support multicast invocation.
- Action is used for event handling, while basic delegates are used for method invocations.
- Action is a built-in delegate, while basic delegates need to be defined explicitly. **(correct)**

### Q2. How are events and delegates related in C#?
- Events are a special type of delegate. **(correct)**
- Delegates can be used to subscribe to events.
- Events can be used to invoke delegates.
- Events and delegates are unrelated concepts in C#.
