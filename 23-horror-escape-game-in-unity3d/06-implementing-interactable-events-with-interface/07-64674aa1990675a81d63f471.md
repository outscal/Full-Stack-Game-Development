# [Quiz] Implementation of generic Event Controller

Implementation of a generic Event Controller


## Questions


### Q1. What is the advantage of using a generic type parameter in GameEventController<T> over the non-generic GameEventController?
- Increased type safety **(correct)**
- Better performance
- Simpler event handling
- Improved event serialization

### Q2. In GameEventController<T>, what is the purpose of the InvokeEvent(T type) method?
- Invokes the baseEvent with the provided type as an argument.
- Adds a listener to the baseEvent with the provided type as an argument.
- Removes a listener from the baseEvent with the provided type as an argument.
- Checks if the baseEvent is null and invokes it with the provided type as an argument. **(correct)**
