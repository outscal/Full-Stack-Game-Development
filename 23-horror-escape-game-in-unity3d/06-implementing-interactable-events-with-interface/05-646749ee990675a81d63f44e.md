# [Quiz] Why Generic Action<T> for OnKeyPickedUp event

Why Generic Action<T> for OnKeyPickedUp event


## Questions


### Q1. What is the purpose of the non-generic GameEventController class in the provided script?
- To handle events without any parameters. **(correct)**
- To handle events with generic type parameters.
- To provide a base class for GameEventController<T>.
- It has no purpose and can be removed from the script.

### Q2. Which scenario would be appropriate for using GameEventController<T> instead of GameEventController?
- When the event needs to carry specific data or parameters.
- When there are multiple event types with different parameters. **(correct)**
- When the event is a simple notification without any parameters.
- When the event needs to be serialized by Unity.
