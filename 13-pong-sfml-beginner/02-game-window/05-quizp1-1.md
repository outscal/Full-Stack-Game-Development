# ⏳Quiz

quiz


## Questions


### Q1. Which sf::Style option should a developer choose if they want the game window to allow resizing while still displaying a title bar?
- sf::Style::Fullscreen
- sf::Style::Default **(correct)**
- sf::Style::None
- sf::Style::Resize

### Q2. When initializing an sf::RenderWindow with a specific sf::VideoMode, how does selecting a higher resolution primarily affect the game's rendering performance?
- It increases the computational load, potentially reducing rendering performance. **(correct)**
- It decreases memory usage, improving performance.
-  It has no significant impact on rendering performance.
- It automatically optimizes frame rates for better performance.

### Q3. What is the primary purpose of sf::VideoMode in the context of sf::RenderWindow?
-  To define the width, height, and color depth of the window. **(correct)**
- To manage the game's frame rate and synchronization.
- To handle user input and window events.
- To control the rendering order of graphical elements.

### Q4. How does `sf::RenderWindow` facilitate cross-platform game development in relation to the underlying operating system's windowing system?
- It requires separate implementations for each operating system.
- It automatically adjusts game mechanics based on the OS.
- It abstracts OS-specific windowing details, allowing for platform-independent code. **(correct)**
- It optimizes graphics settings uniquely for each platform.

### Q5. In the context of sf::VideoMode, what does the bitsPerPixel parameter specify?
- The number of pixels displayed on the screen.
-  The number of color channels used in rendering.
- The color depth, indicating how many bits are used to represent each pixel's color. **(correct)**
- The refresh rate of the display in Hertz.
