# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. Why is Quick Sort considered an in-place sorting algorithm?
- It requires additional space proportional to the size of the input array
- It sorts elements within the original array without needing extra space for another array **(correct)**
- It uses auxiliary arrays for sorting
- It creates new arrays to hold sorted elements

### Q2. In Quick Sort, what happens during the partitioning step?
- Elements are merged into a sorted array
- Elements smaller than the pivot are moved to the left, and larger elements to the right **(correct)**
- Elements are inserted into their correct positions one at a time
- Adjacent elements are swapped if they are in the wrong order

### Q3. Suppose you are sorting an array of eight integers using quicksort, and you have just finished the first partitioning with the array looking like this: 2 5 1 7 9 12 11 10. Which statement is correct?
- The pivot could be 7, but it is not 9.
- The pivot is not 7, but it could be 9.
- The pivot could be either 7 or 9. **(correct)**
- Neither 7 nor 9 is the pivot.

### Q4. In the Quick Sort algorithm, if the array is [3, 6, 8, 10, 1, 2, 1], and the pivot is chosen as the last element, what is the pivot's final position after the first partitioning?
- 0
- 1 **(correct)**
- 5
- 6

### Q5. Which scenario can significantly impact the performance of Quick Sort, making it inefficient?
- Sorting a small dataset
- Sorting an array with many duplicate elements **(correct)**
- Sorting a large, randomly ordered dataset
- Sorting an already sorted array
