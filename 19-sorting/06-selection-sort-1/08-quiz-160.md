# [⏳Quiz]

[⏳Quiz]


## Questions


### Q1. How does Insertion Sort differ from Selection Sort in terms of its sorting process?
- Insertion Sort divides the array into a sorted and unsorted section, while Selection Sort repeatedly selects the smallest element. **(correct)**
- Insertion Sort sorts the array by swapping elements, while Selection Sort uses merging.
- Insertion Sort sorts by comparing adjacent elements, while Selection Sort uses heap data structures.
- Insertion Sort requires more additional memory than Selection Sort.

### Q2. If an array of length n is sorted in ascending order, how many comparisons will Selection Sort make?
- 0
- n
- n^2
- n*(n-1)/2 **(correct)**

### Q3. Why is Selection Sort considered a non-adaptive algorithm?
- It performs the same number of comparisons regardless of the initial order of the elements. **(correct)**
- It changes the relative order of equal elements.
- It requires additional memory for sorting.
- It has a time complexity of O(n log n).

### Q4. What is the significance of the min_index variable
- It keeps track of the current element being compared.
-  It stores the index of the smallest element found in the unsorted portion of the array. **(correct)**
- It stores the value of smallest element in the sorted portion of the array.
- It stores the value of smallest element in the unsorted portion of the array.

### Q5. You are developing a game where characters need to be sorted by their attack power in ascending order. The attack powers are given as [15, 25, 5, 20]. Using Selection Sort, how many swaps are required to sort this list?
- 2
- 3 **(correct)**
- 4
- 5

### Q6. Which of the following sorting algorithms is considered unstable?
- Bubble Sort
- Insertion Sort
- Selection Sort **(correct)**
- All of the above

### Q7. Given the array [4, 3, 2, 1], which sorting algorithm would result in the most number of swaps to sort the array in ascending order?
- Bubble Sort **(correct)**
- Insertion Sort
- Selection Sort
- All perform the same number of swaps

### Q8. What is the auxiliary space complexity for Selection Sort?
- O(n)
- O(n log n)
- O(n^2)
- O(1) **(correct)**
